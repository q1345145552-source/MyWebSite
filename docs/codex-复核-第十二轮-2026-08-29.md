# 请复核（第十二轮）：上一轮 3 组 P1 + 3 条 P2 全修完

**你上一轮报的每一条都属实。三条里有两条是同一个病根 ——「我只修了表面」：**

- 派生值闸：我只比了**算出来的原值**，没比**四舍五入后真正落库的值**。
  你用 `0.01×0.01×0.01 cm` 打穿下边界（落库变 0 方 = 按方数收费等于白送），
  用 `9999.9999996` 打穿上边界。
- 「确认收货不能写 0」：我上一轮加了 `n > 0` 就以为修好了，
  你换成 `0.001` 照样存成 0.00。这一轮才按列精度改。

**基线（你上轮看的终点）**：`cd0b94c`
**这一轮**：`6dba858`
**范围**：10 个文件、+361 / −82。**没有动数据库、没有迁移、没有改任何数据。**

```
git log --oneline cd0b94c..HEAD
git diff cd0b94c..HEAD
```

---

## ⛔ 不许做的

1. **不许部署、不许推送、不许跑迁移、不许写任何数据。不许改仓库里任何文件。**
2. **不许在页面上做会改数据的操作。不许调真的 DeepSeek。不许查生产库。**
3. **不许改这几个已拍板的口径**：北京时间；统计类不润色；
   登录限流 30 分钟 20 次；`/images/*` 无认证不修。
4. **不许为了让测试变绿去放宽断言。**

---

## 一、这一轮改了什么

### 1.1 派生值按**舍入后**判，两头都卡
先按列精度舍一次，再拿舍完的数判上下界。
下边界的提示语专门说清后果：「存进系统会变成 0」。

### 1.2 确认收货前后端口径对齐
- 产品数量：空着 = 不发；填了必须正整数（跟后端一致）
- **负数/乱填的重量体积不许被静默吞掉** —— 上一版对负数返回 `undefined` =「当没填」，
  员工填 -5kg 页面提示**成功**、库里还是旧重量，他以为改了其实没改
- 管理员预报单页那四个框也改成「0 显示成空框」（上轮我只改了员工端）

### 1.3 金额和跨行汇总
在 `recalcPrealertFee` / `recalcCustomerTotals` 里加闸（**写库之前**），
抛 `BusinessError` 翻成 400。

### 1.4 🆕 锁序假绿：换了思路，不再枚举
你这一轮用 `if (Boolean(0))` 又绕过去了 —— **第三次找到新写法**
（`false` → `0`/`!true` → `1===2` → `Boolean(0)`）。
我意识到 `!!0`、`[].length`、`Number('')`、`(() => false)()` …… **枚举是追不完的**。

改成：**加锁那一行不许挂任何条件**。锁本来就该无条件拿；
真实的条件锁写成块形式，由大括号配对处理。
**七种假条件实测全部被逮住。**

⚠️ 顺带发现一个自己坑自己的：第 7 项有**自己一套扫描**、没跟着用新判断，
于是我改完 `scanFile` 之后七种仍然全绿。
**同一个规则写在两处，改一处就会漏。**

### 1.5 P2
- 普通版集货收数字字符串，校验时转过、**写库那三处用的还是原值**。
  共用校验函数现在就地把规范化后的值写回
- **删掉 `receivePrealert`（57 行死代码）**。我上一轮还专门给它补了校验 ——
  补在永远不执行的代码上，纯属白做
- `git diff --check` 那个末尾空行

---

## 二、请你重点打的

1. **1.1 那个「先舍再判」对不对。**
   `Number(value.toFixed(scale))` 在浮点上可靠吗？
   请拿边界值打它（0.0000005、9999.9999995、负数、极大值、
   以及 `toFixed` 本身在 JS 里的舍入怪癖）。
2. **1.3 那两个汇总闸放在写库之前，但它们在事务里。**
   抛错会回滚整个事务 —— 这个行为对吗？
   有没有正当场景是「这一张预报单金额超限，但其它的应该照常保存」？
3. **1.2 的语义变化**：现在「空着 / 0 = 不发这个字段」。
   那**员工想主动把重量清零**（比如之前填错了）怎么办？
   我认为「收到的货不可能是 0 公斤」所以不该有这个需求 —— **请你评价这个判断**。
4. **1.4 那条新规矩会不会误伤**：仓库里有没有正当的「单行条件锁」被我挡了？
   请把所有 `FOR UPDATE` 的行过一遍。
5. **再想新的绕法**打这 12 个脚本。你已经想了 14 种，我都补了。
   特别是那几条**扫源码**的（集货删除接线、前端第 6/9 项、
   锁序第 8/9 项、金额第 11 项）—— 它们天生只能证明「源码里写了」。
6. **找我这份清单之外的。**

---

## 三、我做过的验证

- **12 个自测脚本、235 项**，退出码全 0，全部已接进 CI
- 三套 TypeScript 检查 0 错；Web 构建 25/25 页
- **变异验证 12 个**，全部会红，含你报的 3 个 +
  **七种假条件**（`Boolean(0)` / `!!0` / `[].length` / `1===2` /
  `false` / `Number('')` / `(() => false)()`）

**我没做的：**
- 任何真实并发测试（本地跑不动）
- 任何真实 DeepSeek 调用

**这一轮我自己踩的坑：**
- 第 7 项有自己一套扫描没跟着改，导致我以为修好了、七种仍全绿 ——
  **同一个规则写在两处，改一处就会漏**

## 四、还没修的（等老板拍板，已经挂了几轮）
- 那 4 张运行期图片的 blob 仍在提交历史里（425KB），要抹掉得重写分支历史
- 登录时间侧信道：你建议的「异步 scrypt + 并发上限 + 虚拟验证」
- 父单能不能派送：你建议「`packageCount > 0` 可派、`= 0` 且已有子单不可派」

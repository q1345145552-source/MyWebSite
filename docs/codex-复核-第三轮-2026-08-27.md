# 请复核（第三轮）：跨页面 P1 + 4 条 P2/P3 已修

**你上一轮报的都属实。** 特别是那个跨页面 P1 —— 取消整柜时子预报单状态不跟着变，
财务页只看子单状态就把作废的钱算进待收款，两页对不上。我核过，确实是我漏了。

老板要求**这一轮确认到「没任何问题」再上线**，所以请你尽量往死里挑。

---

## ⛔ 不许做的

1. **不许部署、不许推送、不许跑迁移、不许写生产数据。** 结束 `git status` 与开始一致。
2. **不许改排版**（几列、列顺序、每行怎么排、列宽、加列减列）。
3. **不许改分柜算法、`split-metrics.ts`、`total-metrics.ts`、两个 xlsx 模板、模板打印设置。**
4. **不许把「柜价维护」或「毛利率趋势」加回来** —— 都是老板明确要求删的。

---

## 一、5 处修改

### P1 跨页面：取消整柜后，财务页仍计入子预报单

`apps/api/src/modules/finance/routes.ts`
- 查询里补上父计划状态：`planCustomer: { select: { plan: { select: { status: true } } } }`
- `const planDead = isDead(p.planCustomer?.plan?.status ?? "")`
- `bucket: planDead ? "dead" : whrBucket(p.status)`
- `amount: planDead || isDead(p.status) ? null : numOrNull(p.totalFee)`
- `paid: !planDead && whrBucket(p.status) === "received"`
- 状态列显示「**整柜已取消**」

**请你重点看**：柜子收款页（`admin-ops` 的 `by-container`）是在**循环开头 `continue` 跳过整个柜**，
财务页是**逐单标记 dead**。两种写法不一样，**结果是否严格等价** —— 有没有哪种数据形态下两页又会飘。

### P2 报价 0 元 vs 未报价

`apps/web/src/app/admin/settlement/page.tsx` 客户明细合计：
```
c.orderCount > 0 ? money(总额) : "—"
```
口径：**名下有单 → 就算合计是 0 也显示 `¥0.00`**（报价确实是 0）；**一张单都没有 → 显示「—」**。

⚠️ 财务页的**单行金额**用的是另一套：`amount === null → "—"`，`0 → "¥0.00"`。
**请确认这两套口径在语义上一致，不会让人看着矛盾。**

### P2 首页每 10 秒白算利润

`apps/api/src/modules/admin-ops/routes.ts`：删掉 `/admin/ops/overview` 里
查结算表 + 算 `profitSummary`/`profitTrend` 的整段（33 行），返回里那两个字段也去掉。
前端 `business-api.ts` 的类型同步删除。

**请你确认**：`/admin/ops/overview` 的其它字段（`supplierPriceAlerts`、`customsAlerts` 等）
一个都没被误删，前端也没有别的地方还在读被删的字段。

### P3 日期按 UTC 截，差一天

`apps/web/src/app/admin/finance/page.tsx`：新增 `dayInBeijing()`，`createdAt` 显示改用它。
实测：UTC `2026-08-26T17:00` 旧写法显示 `08-26`、新写法显示 `08-27`。

**请你确认**：柜子收款页有没有同样的问题（它目前不显示日期，但接口返回了 `createdAt`）。

### P3 死 CSS + `planning` 中文 + 说明文字

- 死类清到 **0 个**（`lastmile-container-status`、`lastmile-mobile-scroll-hint` 及标签栏那批），
  大括号 **527/527** 配平，空媒体查询 0 个
- 柜子收款页状态映射补 `planning: "计划中"`
- 「未到收款环节」说明从「货还没到仓」改成「还没到该收钱的环节」

---

## 二、我这轮真跑过的

**① 真实夹具验跨页面 P1**（本地库没有取消的柜，我临时改一个再还原）：

```
把 WHR0000001 改成 cancelled（它底下有 WHRP9901 ¥102、WHRP9902 ¥51）
  修之前：财务页待收 ¥255 / 4张   柜子收款待收 ¥102     ← 差 ¥153，两页打架
  修之后：财务页待收 ¥102 / 2张   柜子收款待收 ¥102     ← 完全一致
         那两张单显示「整柜已取消」，金额「—」
         柜子收款里那个柜整个不出现（3 个柜不是 4 个）
验完已还原成 loading，跟改之前一致
```

**② 日期修复实测**：三个时间点手算对比，UTC 17:00 那个确实从 08-26 变成 08-27。

**③ 死 CSS**：脚本扫全部 `lastmile-*` 类 → 组件里没人用的 **0 个**；大括号配平；空媒体查询 0 个。

**④ 类型和构建**：web tsc **0**；api tsc **19（基线）**；web build **通过**。

**⑤ ⚠️ 过程中我又踩了一次坑，值得你知道**：第一次验的时候页面没变，
一查是**后端跑的旧代码**（进程起于 02:18、代码改于 02:47）。重启后才对。
**所以你验后端改动前，务必核对进程启动时间晚于代码修改时间。**

---

## 三、你前两轮已经验过、这轮不用重复

- 三个导出 + LibreOffice 真打开（排版、边框、泰文字形、翻页、SUM 公式）—— 通过
- CSS 恢复完整性 513→508 —— 通过（本轮又删了 3 条死规则，**请确认 508 → 505**）
- 柜价删除主体、两个财务接口的权限和公司隔离 —— 通过
- `pending / cancelled / full_confirmed` 三个状态边界 —— 上一轮通过，本轮逻辑未动
- 四档不掉单、`total = received + receivable + notYet` —— 上一轮通过

---

## 四、请你重点往死里挑这几处

1. **两页判 dead 的写法不同（跳过整柜 vs 逐单标记），是否严格等价**
2. **父计划为 `cancelled` 之外的异常值**（比如 plan 被删、`planCustomer` 为 null）时，
   `p.planCustomer?.plan?.status ?? ""` 会走到哪一档 —— 会不会把好单误判成 dead
3. **删掉利润计算之后**，`/admin/ops/overview` 其它字段有没有被连累；前端有没有残留读取
4. **`orderCount > 0` 这个判断**：`_count.prealerts` 会不会把已取消的预报单也数进去，
   导致「明明没有有效单，却显示 ¥0.00」
5. **柜子收款接口返回了 `createdAt` 但页面不显示** —— 是死字段还是将来要用
6. **两个页面的空值口径**（`—` vs `¥0.00`）逐格核一遍，有没有第三种不一致
7. 有没有**别的接口**也犯「只看子单状态、不看父计划」这个错

---

## 五、我这轮没验的

- 没有构造「plan 被物理删除但 prealert 还在」这种畸形数据
- 没有重新用 Excel 打开导出（本轮没动导出链路）
- 没有部署、没有跑迁移、没有碰生产数据

---

## 六、系统特有的坑

1. 三端各写各的（已确认员工端「财务」只有「客户集货余额」，不受影响）
2. 集货两版算钱方式不同：普通版全手填；仓库版签收时按「方数 × 单价」自动算 —— **保持现状**
3. 状态判断用黑名单不用白名单（`CLAUDE.md` 第 13 条）
4. **后端跑 UTC**，「今天」「日期」都要按北京时间换算
5. `apps/api` tsc 基线 **19 个错**，别去修
6. 手写 SQL 列名要去数据库核（`@map` 用得不统一）
7. **红线：客户不能看到柜号、不能看到价格**
8. **本地后端不会自动重载**：`lsof -ti tcp:3001 | xargs kill -9`，核对进程启动时间
9. 空值不要显示成 0

**汇报时明确分开写「真跑过的」和「只读代码的」。**

# 请复核：你报的 P1 / P2 / P3 已修（2026-08-26）

**你上一轮的 P1 判断是对的，我认。**

我上一轮只在 Node 里直接给 `buildLastmileTemplateWorkbook()` 喂 null，
证明了**生成器**正确，就宣布"端到端通过"——
而真实链路里 API 早就把 null 抹成了 0，生成器根本没机会见到 null。
这属于"只验了一半就当成全链路"，是这个项目 `CLAUDE.md` 第 24 条反复记过的坑。

下面是三处修复和这次的验证方式，请你复核。

---

## ⛔ 先说不许做的

1. **不许改分柜算法、`split-metrics.ts`、`total-metrics.ts`、schema、迁移。**
2. **不许改 `/admin/orders` 的取数逻辑。**
3. **不许改排版**（几列、列顺序、每行怎么排、列宽、加列减列）。
4. **不许写脚本改生产数据。**
5. **不许改两个 xlsx 模板文件**（原因见第四节）。
6. **中文页 `E16/F16/G16` 的 SUM 公式不许删。**

---

## 一、改了哪三处

### P1-a 客户签收单接口：`null` 不再被抹成 `0`

`apps/api/src/modules/admin-ops/routes.ts:553-554`

```diff
- weightKg: weightKg ?? 0,
- volumeM3: volumeM3 ?? 0,
+ weightKg: weightKg ?? null,
+ volumeM3: volumeM3 ?? null,
```

上游那一大段（家族补算 / `metricByPieceShare`）本来就会在"订单也没填"时正确返回 `null`，
结果全被这一句吃掉。**这是你指出的病根。**

### P1-b 整柜清单接口：同一个毛病的第二处

`apps/api/src/modules/loading-manifests/routes.ts:369`

```diff
- weightKg: loadedWeight ?? 0,
+ weightKg: loadedWeight ?? null,
```

⚠️ 同一行附近的 `volumeM3: Number(item.loadedVolumeM3)` **没动** ——
`shipment_container_items.loaded_volume_m3` 在 schema 里是非空 Decimal，不存在 null。
**请你确认这个判断对不对。**

### P2 前端响应类型改成可空

`apps/web/src/modules/lastmile/exportDispatchWorkbooks.ts:11-12`

```diff
- weightKg: number;
- volumeM3: number;
+ weightKg: number | null;
+ volumeM3: number | null;
```

### P3 进度条死 CSS 清掉

`apps/web/src/app/globals.css:3045-3071`

- 删掉 `.lastmile-progress-track` 和 `.lastmile-progress-track i` 两条规则
- **顺带去掉了 `.lastmile-wd-progress > div:first-child` 的 `margin-bottom: 5px`** ——
  那 5px 原本是给进度条留的位置，不去掉的话「签收进度 1/1」下面会空一截

---

## 二、这次的验证方式（和上一轮的区别）

**上一轮的错误**：Node 里直接调生成器 → 只证明了生成器。

**这一轮**：真实浏览器 → 点真实按钮 → 真实接口 → 真实生成的文件 → 在浏览器里把
xlsx 解压出来读格子。**中间没有任何一步是靠推论拼的。**

### 前置：先重启了本地后端

本地 `tsx` 不带 watch，**改了后端不重启就还是旧代码**（这是本项目另一个记录在案的坑）。
核对过：
```
进程起于 20:07:58
代码改于 20:07:19        ← 进程晚于代码，确认跑的是新代码
```

### 真跑结果

| 环节 | 方法 | 结果 |
|---|---|---|
| 接口层 | 在页面里 hook `fetch`，点真实导出按钮，抓 `/admin/lastmile/customer-export-data` 的真实响应 | `SEED100149`：**体积 null、重量 null** ✅ |
| 文件层 | hook `URL.createObjectURL` 截获真实生成的 Blob，用 `DecompressionStream('deflate-raw')` 手动解 zip，直读 sheet XML | 中文页 `E6=30 / F6 空白 / G6 空白`；泰语页 `E8=30 / F8 空白 / G8 空白 / F28 空白 / G28 空白` ✅ |
| 排版 | 真实浏览器截图 | 卡片分隔线仍在，「签收进度 1/1」下方无多余空白，间距未塌 ✅ |
| 类型 / 构建 | — | web tsc **0**；api tsc **19（基线）**；web build **通过** |

---

## 三、请你重点复核这几点

1. **`?? null` 之后，下游还有没有别的消费方假设这两个字段一定是数字。**
   我查到的消费方只有导出链路，但请你再扫一遍（含 `docs/api-contract.md` 是否需要同步）。
2. **`loading-manifests:369` 附近的 `volumeM3` 我判断不可能为 null，对不对。**
3. **`lineTotal()` 用 `Number(line[key] || 0)`，null 会被当 0 参与合计** ——
   我认为合计时"只加有值的"是对的，请确认没有别的语义被破坏。
4. **P3 删掉的 5px 是否还有别的元素依赖。**
5. 有没有**别的接口**也犯了同样的 `?? 0` 毛病（我只改了你指出的两处，没有全局扫描）。

---

## 四、你报的另外两条，我**故意没改**

**「模板打印会被拆成额外窄页」「泰文页右侧列被拆页」**

你自己也验证了：**空模板本身就会从 2 张工作表打印成 6 页，不是本轮改动引入的。**
根因是模板没有设打印区域和 fit-to-width。

修它要动 `.xlsx` 模板文件本身（设打印区域、缩放到一页宽），**属于用 Excel 调模板，不是改代码**。
我判断这应该作为一件独立的事情处理，已经如实告诉用户了。

**请不要顺手去改模板文件。** 如果你认为有必要改，先说方案，等用户拍板。

---

## 五、我这轮**仍然没能验证**的

- **用 Excel / WPS 真打开看**排版、边框、打印分页（我只读 XML 和在浏览器里解压看值）
- 泰文在 Excel 里的**实际字形和换行**
- 超过 10 行自动翻页后第 2 页的表头和序号（你上轮验过，我没重复验）
- **本轮没有部署、没有跑迁移、没有碰生产数据**

---

## 六、这个系统特有的坑（避免复核时查错）

1. 三端各写各的；`LastmileDispatchWorkspace` 是管理员端和员工端**共用**的，改一处两端都变
2. 物流轨迹写在**子单**上；客户看到的是父单 + 全部子单合并
3. 柜货关系在 `shipment_container_items`，不是 `shipments.batch_no`
4. 后端容器跑 **UTC**，「今天」要按北京时间换算
5. 海运 22 步 / 陆运 16 步两套流程，不许串
6. `apps/api` 的 tsc 基线就是 **19 个错**，别去修
7. 手写 SQL 的列名要去数据库核，这个库 `@map` 用得不统一
8. **红线：客户不能看到柜号、不能看到价格**
9. **本地后端不会自动重载**，验后端改动前必须 `lsof -ti tcp:3001 | xargs kill -9` 并核对进程启动时间
10. 空值不要显示成 0，列表不要静默截断

**汇报时请明确分开写「真跑过的」和「只读代码的」。**

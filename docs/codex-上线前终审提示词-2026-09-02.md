# Codex 上线前终审提示词 — 竞态全修 + 已入库状态（2026-09-02）

> 用法：整段复制给 Codex，或让它读本文件照做。

---

## 提示词正文

你是湘泰物流系统的独立审查员，做上线前终审。已部署版本 `177c9e6`，本地 main 领先 4 个提交，全部待上线：

1. `f9f60d3` 客户端「执行查询」慢响应盖列表的竞态首修（你上一轮判定「修复不完整」的那笔）
2. `80c1578` 你那轮复核的提示词文档
3. `729388d` **按你的全扫结果修 28 处**：统一引入 `apps/web/src/modules/shared/request-gate.ts`（领号验号 + 认主人两种用法），你点名的 3 个缺口（catch/finally 验号、筛选条件竞态、挂载兜底）和 24 处同类病全部换装；另含员工建单轨迹下一站「国内仓」→「装柜」
4. `0ede773` **运单流程新增「已入库 inWarehouseCN」状态**（老板拍板：货到仓才录单）——员工建单直接已入库、客户预报单仍从已创建起步、确认收货推到已入库、卸柜还货退回已入库；老单不回填

审 `git diff 177c9e6..HEAD`。

### 任务一：对着你自己的全扫清单逐条销号

你上一轮列了「任务一 3 缺口 + 任务二 24 处」的表。逐行核对本次 diff：每一处是否真装上了防线、防线是否覆盖成功/失败/finally 三分支、认主人的 ref 是否在用户点击处同步赋值（不是等 React 提交）。特别抽查你当时给过具体触发序列的几处：ShippingConfig 保存前核对归属、删除预检展示与执行同 id、imports 提交锁、FclInquiryPanel 表单锁、whr 签收照片认主人。发现「装了但挡不住」的写清触发序列。

### 任务二：审「已入库」状态改动（这是状态机手术，往死里查）

1. **两张流程表的对应关系**：shipments/status-flow.ts 插入了 inWarehouseCN（海运 23 步/陆运 17 步），主修声称柜↔运单对应走 CONTAINER_TO_SHIPMENT_STATUS 名字映射、无任何按下标互查——亲自 grep 验证这个结论，再核柜子推进批量改运单、装柜置 loaded、手动改状态三条路对新状态的行为。
2. **口径一致性**：客户端四分类 pending（created/inWarehouseCN/holdLoading）、顶部数字「未发出」（同三个）、shared-types 的 NOT_IN_TRANSIT / IN_TRANSIT 推导、AI 对照表与 seed 手抄副本逐字一致、三端筛选下拉——逐处对。
3. **行为对**：客户预报单创建路径一字未动？确认收货只在锁后重读为 created 时才推（已装柜的绝不拽回）？卸柜两份实现（unload-item.ts 与 loading-manifests 手抄款）行为一致（test-unload 第 8 项的源码对表是否真防线）？删「已入库」轨迹、轨迹弹窗颜色、statusToneClass 这些边角有没有漏。
4. **三道闸**：只往前不往后、最后一步不自动结单、补充资料必须停——有没有被破坏。

### 任务三：回归面

这 4 个提交有没有互相打架或改坏 08-31/09-01 已修好的行为（五分类按钮、翻页归位、立即查询、预报单分页搜索池、钱包分页）。

### 规矩与输出

可跑：三套 tsc --noEmit、15 套 npm run test:*（不连库）、next build、无库夹具；不许连库、不许起服务、不许 git 写操作、不许改文件。已拍板/待拍板清单同前（新增一条待拍板不要报：ai_status_labels 库里 inWarehouseCN 行的中文改名，等老板点头单独做）。

输出：① 全扫清单逐行销号表（✅/❌+依据）；② 已入库改动核查表；③ 新问题清单（P1/P2/P3 + file:line + 触发序列 + 修法），没有就明说；④ 实跑记录；⑤ 最终一句话：可以部署 / 暂缓部署。

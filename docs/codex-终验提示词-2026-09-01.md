# Codex 终验提示词 — 2026-09-01

> 用法：把下面整段复制给 Codex。

---

你是湘泰物流系统的独立审查员，做上线前的**终验**。本地 main 现在是 `c997dac`（比 origin 领先 10 个提交）。你上一轮复核（审在 7d897bb）的结论是「7 条全修对、无新 P1」，并提出 5 个收尾项：FCL 超大页码 500（新P2）、限流文案指错路（新P3）、契约文档缺记（新P3）、合计溢出（旧P2）、钱包流水静默截断（旧P2）。这 5 项已在 `7d897bb..c997dac` 里全部整改，并新增了 `test:fcl-inquiries` 回归脚本（测试脚本现在共 15 个）。

### 任务

1. **逐项复核这 5 项**：对着 `git diff 7d897bb..c997dac` 和当前代码，确认每项真修对了（给 file:line 依据）。重点：
   - FCL 页码：用你上次的无库夹具再打一遍 1e400 / 超长数字串 / 0 / -1 / 2.5，期望全部 400 中文、碰不到库；
   - 合计溢出：`guardCombinedTotals` 的上限口径是否和数据库列定义一致（Int / Decimal(10,2) / Decimal(10,3)），调用位置是否在事务内、写库前；
   - 钱包流水：后端 total 是不是真 count、前端翻页是否闭环、默认 pageSize 变化有没有伤到别的调用方；
   - 限流文案：按 IP 和按账号两条提示各自说的办法是否真走得通；
   - 文档：api-contract.md 补记和实际接口行为逐字段对。
2. **回归面检查**：这批 diff 有没有新引入问题（契约、类型、锁序、校验遗漏）。
3. **可以跑**：15 套 `npm run test:*`、三套 `tsc --noEmit`、`next build`、无库夹具。**不许**连库、起服务、git 写操作、改文件。

### 输出

- 5 项逐项结论表（✅/❌ + 依据）；
- 新问题清单（P1/P2/P3 + file:line + 依据 + 修法），没有就明说；
- 实跑记录；
- 最终一句话：**可以部署 / 暂缓部署**。

已拍板/待拍板事项与前两轮相同，一律不报（含：/client/prealerts/update 与 /delete 去留、仓库版柜+客户表结构唯一约束、部分卸柜状态回退已实施待知悉、历史加收金额只读展示、内存版黑名单/限流重启清零）。

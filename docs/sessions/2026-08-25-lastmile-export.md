# 2026-08-25 尾端派送分组与导出

## 最终状态（已按更正后的两个业务入口完成）

2026-08-25 用户最终确认：

- 整柜清单就是给尾端拆柜仓的模板，在“装柜管理”按柜导出；
- 客户派送/签收单在“尾端派送”建立 WD 后，从该 WD 选一个客户导出；
- 一张 WD 仍表示一辆车的一趟路线，可含多客户、多地址、多运单；
- 客户模板任何位置均不输出柜号。

当前已完成并验证：

- `GET /staff/loading-manifests/export-data?id=...`：公司隔离，一条实际装柜记录导出一行，空柜阻止导出，不读取 WD/车辆。
- `GET /admin/lastmile/customer-export-data?deliveryNo=...&clientId=...`：服务端精确限定所选客户，不查柜号，其它客户返回 404。
- 员工端装柜管理已加“导出整柜派送清单”；管理员/员工端尾端派送都以 WD 卡片展示并可选客户导出。
- 两份模板的列宽、行高、样式、合并、公式、页边距和打印设置均经 OOXML 比对保持。
- 演示文件在 `outputs/container-and-wd-export-demo-2026-08-25/`；补丁、验证和回滚在 `.audit/2026-08-25/container-and-wd-exports/`。
- 前端生产构建通过；API 类型错误数仍为基线 19，本次新端点区间为 0。未部署生产。

### 2026-08-26 P1 / P2 / P3 修复复核

- 本轮三处修复均通过：两个导出 API 不再把未知明细指标改成 0，Web DTO 已可空，进度条死 CSS 与多余 5px 已清除。
- 当前路由处理器到当前 builder 的 XLSX 定向实跑确认 null 留空、显式 0 保留；中文 `E16/F16/G16` 公式仍在。
- `loaded_volume_m3` 经 schema、迁移和本地数据库只读查询确认非空，15 条实际记录无 null；`lineTotal()` 只在合计中把 null 当加法 0，语义正确。
- 额外发现一个与本轮无关的既有 P2：财务汇总逐行把未知订单重量/体积显示为 0；另有导出 nullable 契约文档缺口和未挂载轨迹组件的低优先级风险。
- 本次未改业务源码、XLSX 模板、数据库结构或业务数据；记录在 `.audit/2026-08-26/p1p2p3-rereview/`。

### 2026-08-26 UI 三处与导出留空复查

- UI 空元数据行、去进度条和柜价按钮主逻辑通过；真实浏览器确认全空行收起后分隔线仍在，10 票 WD 完整显示。
- 发现留空功能未贯通真实导出链路：客户导出 API 在进入 builder 前仍将 null 体积/重量转为 0；整柜导出的缺失重量也同样转 0；手写前端类型仍错误声明为非空 number。
- LibreOffice 真实打开 10 票/11 明细文件：自动续页、序号、中文 SUM、泰文字形和 null 格子均通过；但正式模板本身缺打印区域/fit-to-width，4 张逻辑工作表会在 LibreOffice 拆成 12 张 A4。
- 进度条 JSX 已删，但 CSS 留有两条死规则和文字下方 5px 空白。本次只复查，未改源码、模板或业务数据；证据在 `.audit/2026-08-26/ui-export-review/`。

### 2026-08-25 晚间本地全链路复测

- 用真实 localhost HTTP + 签名 JWT + 进程内 Prisma 事务夹具跑通“整柜 GET → XLSX”和“POST 创建 WD000501 → 按 CLIENT-A GET → XLSX”；未连接或写入数据库。
- 复测发现并修复三处：泰文短页/续页空白行残留模板假序号；客户备注中的柜号进入签收单；创建 WD 未校验运单 `companyId`。
- 跨公司混合运单现在整车返回 404 并原子回滚；客户备注只保留“已装柜（分装 N件）”，不带柜号。
- 正式模板生成的整柜 26 行/2 页、客户 11 行/2 地址/4 个中泰工作表已通过 OOXML、公式、Artifact Tool 全页渲染和 LibreOffice 打开；模板样式、合并、间距及打印设置不变。
- 可查看文件在 `outputs/two-export-local-retest-20260825/`；统一验证、补丁和回滚在 `.audit/2026-08-25/two-export-local-retest/`。未部署。

### 本地系统打开后的数据恢复

- 页面首次打开看似无数据，API 实际因 `container_lastmile_prices` 尚未建表持续 500，Chrome 会话也已过期。
- 已执行仓库现有的纯新增表迁移并登记 applied；随后刷新独立本地管理员浏览器会话。
- 修后 `/admin/lastmile/orders` 返回 8 行、柜价接口和装柜接口均返回 18 柜；真实 DOM 可见 6 个 WD、4 个柜号，无空态或错误提示。
- 记录：`.audit/2026-08-25/local-system-no-data/verification.md`。

### 尾端派送 UI 工作台优化

- 管理员、员工端已统一成共享工作台，业务层级固定为 `WD 一趟车 → 客户 → 收货地址/运单`；柜价维护从派送作业首屏拆到管理员独立页签。
- 搜索在完整 WD 分组之后执行，命中任意一票仍保留整张 WD 和真实签收进度；“追加运单”使用独立选择状态并明确显示目标 WD。
- 客户组直接提供“导出该客户派送单”，实际请求严格带 `deliveryNo + clientId`；客户只读导出仍没有非空柜信息或价格字段。
- 派送列表与柜价列表均区分加载、失败、真实空数据、筛选无结果；删除历史重复派送时会展示后端返回的“仍在另一 WD 派送中”说明。
- 本轮没有修改 XLSX 模板、Excel 生成器、整柜入口、数据库或分柜算法。真实浏览器、HTTP、响应式、Web 类型检查、25/25 构建、补丁和回滚均通过；记录在 `.audit/2026-08-25/lastmile-ui-optimization/`。

### 2026-08-26 映射复核

- Container→柜价、派送记录→WD/客户/地址、staff 收件字段→共享 UI、客户导出 `deliveryNo + clientId` 四条链路均以本地数据库、真实 HTTP 和浏览器核对通过。
- 18 柜全部进入柜价列表，当前实际价格记录为 0；8 条派送记录组成 6 张 WD，155 条可派送运单全部取完，逐字段无 mismatch。
- 清除非 THB 柜价后前端币种立即归一为 THB；`ShipmentItem` 补齐 `receiverNameTh/receiverPhoneTh` 类型声明。
- 临时保存 USD 价格并清除，数据库和页面均原样恢复；验证、补丁和回滚在 `.audit/2026-08-26/lastmile-mapping-check/`。

### 2026-08-26 尾端界面格式收敛

- 业务逻辑保持原样，只把管理员/员工共用工作台从彩色卡片风格收敛为黑白灰工作表：状态改纯文字，进度改灰色，页签改下划线，按钮统一灰色描边，统计字号与卡片层级同步减弱。
- 创建 WD 仍是运单选择 + 车辆资料双栏，派送任务仍按 WD → 客户 → 地址/运单显示；柜价页继续独立，移动端新增宽表滑动提示并固定柜号列。
- 真实浏览器覆盖管理员派送/创建/柜价、员工派送及 1440/390px；修复手机菜单按钮蓝底遮住蓝色菜单线的问题，页面宽度 390/390，无全局横向溢出。
- 功能回归、计算样式回归、Web 0 错类型检查、25/25 生产构建和补丁/回滚均通过；记录在 `.audit/2026-08-26/lastmile-format-simplification/`。未改 API、数据库、XLSX 模板或导出业务。

## 之前回退阶段（历史）

2026-08-25 用户重新确认业务主维度应为“柜”，不是“车/WD”，并要求先撤销本次实现、暂缓重新设计。已完整回退以下内容：

- `GET /admin/lastmile/export-data` 导出接口；
- 管理员端按 WD 卡片分组及整车/客户导出按钮；
- 员工端整车/客户导出按钮；
- XLSX 导出器和 JSZip 直接依赖。

系统恢复为本任务开始前的尾端派送页面和行为；柜号维度的新方案尚未实现。用户随后明确模板不需要回退，因此两份正式 XLSX 仍按原文件逐字节保存在 `apps/web/public/templates/lastmile/`，暂未接入页面或接口。审计记录保留在 `.audit/2026-08-25/lastmile-export/`。

随后用户进一步确认“柜号不能给客户看”。客户签收模板的中文、泰文工作表已删除柜号及柜相关顶部区域，统一改为唛头、电话、收货地址；拆柜仓模板继续保留柜号。客户模板仍暂未接入导出逻辑。

2026-08-25 用户先推进柜维度的价格映射：管理员端尾端派送页面已直接列出装柜管理中的柜子，并可为每个柜子维护一条金额、币种和备注。该功能只建立 `Container → 尾端派送价格` 的一对一关系，与 WD、车辆及导出模板均无关；员工端、客户端不显示。数据库迁移已生成但尚未部署到生产，柜维度导出方案继续等待确认。

## 早期 WD 整车方案（已作废）

- 把 WD 派送单当作“一辆车的一趟路线”展示，一趟可包含多客户、多地址、多运单。
- 管理员端与员工端都可导出整张 WD，也可选择 WD 内的一个客户单独导出。
- 先使用基础版 XLSX；正式模板与字段明天确认。

## 已完成

- API 新增 `GET /admin/lastmile/export-data?deliveryNo=WD...&clientId=...`，按 `companyId + deliveryNo` 隔离，`clientId` 可选。
- 导出数据包含车辆/司机、客户、收件联系方式、每票运单、每票地址、件数/体积/重量、签收进度。
- 整车基础版包含内部来源柜号；单客户版隐藏柜号，带收货签名/异常区。
- 管理员端原平铺表改为按 WD 卡片分组，保留签收、轨迹、删除操作。
- 管理员端和员工端每张 WD 均增加“导出整车”和“选客户 + 导出客户”。

## 验证

- 前端 Next.js 生产构建通过，25 个路由全部生成。
- 本地 API 真实 HTTP 请求：整车和单客户均返回 200，客户范围校验通过。
- 两客户/两地址模拟：整车导出包含两客户；单客户导出仅含选中客户，不含柜号；两张表均已渲染检查。
- API TypeScript 错误数修改前/后均为 19，新端点区间 0 个新错误。
- 完整记录：`.audit/2026-08-25/lastmile-export/verification.md`。

## 正式模板接入历史

功能链路已做完。2026-08-25 下午用户提供两份正式空模板，系统模板已切换：`模板(1).xlsx` 用于整车/拆柜仓，`模板.xlsx` 用于单客户签收。

2026-08-25 已用 `WD009998` 模拟整车示范：2 个客户、3 个收货地址、3 票运单；同时导出 `PANDA-A` 单客户版。两份 XLSX 均已读取、扫描和渲染通过，客户版未出现其他客户、其他地址或内部柜号。示范文件在 `outputs/lastmile-export-demo-2026-08-25/`。

用户进一步确认“原模板的设计、内容和间距都不能动”后，导出器已改为直接读取两份原 XLSX 资产，只替换业务值与按实际数据必须调整的分组合并。源文件与项目模板哈希相同；生成文件的列宽、行高、页边距、打印设置、样式和声明文案已做 OOXML 结构校验，全部保持。严格原模板示范在 `outputs/lastmile-exact-template-demo-2026-08-25/`。

正式模板切换后，整车模板固定数据区变为 10–34 行（25 行），合计行变为第 35 行；首列按新模板填写“序列号”，同一运单的多产品行合并序列号、运单号和备注。正式示范在 `outputs/lastmile-official-template-demo-2026-08-25/`，验证记录在 `.audit/2026-08-25/lastmile-export/new-template-switch/verification.md`。

## 仍待业务确认

- 当前数据库没有封条号，相关值保持空白。
- 单客户同一 WD 有多个地址时，主地址放抬头，其余地址保留在对应货物备注；是否要改成“按地址再拆单”待用户确认。
- 模板和接口形式最终稳定后，再补 ADR/接口文档。

# 请复核：你报的 4 个 P1 已修（2026-08-27 第二轮）

**你上一轮报的 4 个 P1 全部属实，我逐条核过，都是我写的逻辑错。**
另外你顺带提的几条也一并处理了。下面是修法和验证方式，请复核。

⚠️ 你上一轮做得很硬（LibreOffice 真打开验 Excel、CSS 逐规则比对 513 对 513、独立脚本对账），
**那些已经通过的部分不用再验一遍**，见第四节。

---

## ⛔ 先说不许做的

1. **不许部署、不许推送、不许跑迁移、不许写生产数据。** 结束时 `git status` 与开始时一致。
2. **不许改排版**（几列、列顺序、每行怎么排、列宽、加列减列）。
   ⚠️ 例外：本轮我给「柜子收款」表**加了一列「未到收款」**，那是修 P1-1 的必要产物，不是排版改动。
3. **不许改分柜算法、`split-metrics.ts`、`total-metrics.ts`、两个 xlsx 模板。**
4. **不许把「柜价维护」加回来。**
5. **不许改模板打印设置**（拆页那条是模板自带，已知，不修）。

---

## 一、根因：同一条规则我写了两遍

P1-1 和 P1-2 之所以出现，是因为**「怎么算钱」这条规则在财务页和柜子收款页各写了一份**，
两边就飘了 —— 财务页排除了 `pending` 和 `cancelled`，柜子收款页没排除。

**修法：抽成唯一定义处** `apps/api/src/modules/finance/money-rules.ts`（新文件，47 行）

```
whrBucket(status)                            → received | receivable | notYet | dead
taskBucket(status, paymentStatus, hasFee)    → 同上
isDead(status)
```

两个接口都改成调它：
- `apps/api/src/modules/finance/routes.ts:4`
- `apps/api/src/modules/admin-ops/routes.ts:5`

**请你核实**：还有没有别的地方在自己判断这些状态（漏改的话又会飘）。

---

## 二、8 处修改

| # | 你报的 | 修法 |
|---|---|---|
| **P1-1** | 仓库版 `pending` 被柜子收款算进待收款 | `whrBucket` 里 `pending → notYet`；柜子收款接口新增 `notYet` 字段，前端加「未到收款」列 |
| **P1-2** | 已取消的仓库柜仍计入柜子数和金额 | `by-container` 循环开头 `if (isDead(pl.status)) continue;` |
| **P1-3** | 普通版 `full_confirmed` 被算作待收款 | `taskBucket` 里 `TASK_NOT_YET = {collecting, full_confirmed}` |
| **P1-4** | 未报价客户展开显示 `¥0.00` | 明细行合计改成 `> 0 ? money(...) : "—"` |
| 其他 | 财务明细返回带金额的已取消单 | 已取消的行金额改成 `null`（前端显示「—」），避免有人把表里金额加起来跟四个数字对不上 |
| 其他 | 普通版「单数」固定为 1 | 改成 `_count: { select: { prealerts: true } }` 真实条数（生产 `JH0000001` 底下有 2 张） |
| 其他 | 柜价删除后留下死 CSS | 删掉 `.lastmile-admin-tabs` 全部 5 条规则（标签栏已不存在，组件里 0 引用），删完检查大括号配平 |
| 其他 | 管理员首页仍保留「毛利率趋势」和旧利润计算 | 整块删掉（29 行）—— 它指向的「财务结算与利润」已经改成「柜子收款」，不再有利润数据 |

**未处理（有意）**：
- 测试库残留物理表 `container_lastmile_prices` —— 你也确认生产部署不受影响
- `schema.prisma` 一行空行差异 —— 纯格式
- 旧的三个 `/admin/settlement/entries|profit` 接口仍留着（前端已无调用方），本轮不删

---

## 三、我这轮真跑过的

**① 19 种状态组合穷举**（你指出问题都在「本地库尚未出现的状态边界」，所以我直接穷举）：

```
仓库版 8 种：pending / received_pending_payment / payment_submitted / paid /
             loading / shipped / thailand_received / cancelled        → 8/8 正确
普通版 11 种组合（状态 × 付款状态 × 有无金额）                          → 11/11 正确
```
含你报的三个边界：`pending → 未到收款环节`、`cancelled → 不计入`、`full_confirmed → 未到收款环节`。

**② 浏览器实测 P1-4**：本地真登录打开柜子收款，展开未报价客户 `JH0000001`，
明细行显示 `↳ 测试客户 1单 — — — —`，**全是横杠，没有 ¥0.00**；控制台 0 错误。

**③ 死 CSS**：删完 `lastmile-admin-tabs` 剩余 0 处引用，大括号 531/531 配平。

**④ 类型和构建**：web tsc **0**；api tsc **19（基线）**；web build **通过**。

---

## 四、你上一轮已经验过、这轮不用重复的

- 三个导出按钮 + LibreOffice 真打开（排版、边框、泰文字形、翻页、SUM 公式）—— **全部通过**
- CSS 恢复完整性（期望 513 条 / 当前 513 条、缺失 0、孤立选择器 0）—— **通过**
  ⚠️ 但本轮我又删了 5 条 `lastmile-admin-tabs`，**这 5 条请你确认删得对**（应该是 513 → 508）
- 柜价删除主体、两个新财务接口的权限和公司隔离 —— **通过**
- 受保护文件哈希未变 —— **通过**

---

## 五、请你重点复核

1. **还有没有别处在自己判断集货状态**（漏改就会跟共用口径飘）
2. **`notYet` 这一档加进去之后，三个数字加起来是否等于全部**
   （已收 + 待收 + 未到收款 + 已取消 = 总数，不该有单子掉在四档之外）
3. **柜子收款的 `total` 现在 = 已收 + 待收 + 未到收款**，跟财务页的口径是否一致
4. **`_count.prealerts` 是否会把已取消的预报单也数进去**（普通版单数）
5. **删掉「毛利率趋势」之后**，`opsOverview` 里的 `profitTrend`/`profitSummary` 是否变成无人消费的死数据，
   后端还在算它有没有性能代价
6. **删 5 条 CSS 有没有连累别的元素**（`.lastmile-admin-tabs` 曾经和别的选择器共用过一条媒体查询规则）

---

## 六、我这轮没能验证的

- **没有构造 fixture 去跑真实路由**（我是直接测 `money-rules.ts` 的纯函数 + 浏览器看现有数据）。
  你上一轮用夹具跑真实路由抓到了问题，**这轮也请你用同样方式跑一遍**，特别是
  `pending` / `cancelled` / `full_confirmed` 三种本地库没有的状态。
- 没有重新用 Excel 打开导出文件（本轮没动导出链路）
- **没有部署、没有跑迁移、没有碰生产数据**

---

## 七、这个系统特有的坑

1. 三端各写各的（本轮已确认员工端「财务」只有「客户集货余额」，不受影响）
2. **集货两个版本算钱方式不一样**：普通版全手填；仓库版签收时按「方数 × 单价」自动算。
   老板 2026-08-26 拍板**保持现状**
3. 状态判断用**黑名单**不用白名单（`CLAUDE.md` 第 13 条）
4. 后端跑 UTC，「今天」要按北京时间换算
5. `apps/api` tsc 基线就是 19 个错，别去修
6. 手写 SQL 列名要去数据库核（`@map` 用得不统一）
7. **红线：客户不能看到柜号、不能看到价格**
8. **本地后端不会自动重载**：验后端改动前必须 `lsof -ti tcp:3001 | xargs kill -9`，核对进程启动时间晚于代码修改时间
9. 空值不要显示成 0

**汇报时明确分开写「真跑过的」和「只读代码的」。**

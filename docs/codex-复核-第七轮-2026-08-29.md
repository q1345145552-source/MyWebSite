# 请复核（第七轮）：上一轮 3 组阻断项全修完 + 你指出的 4 处假绿全部补实

**你上一轮报的每一条，我都自己复现过，结论：全部属实，一条误报都没有。**

尤其这三条打得很准：

1. **「锁序测试现在只能当提示，不能当并发正确性的证明」** —— 我实测复现了。
   把「推进柜子状态」里那段逐票运单锁**整段删掉**，5 项照样全绿。
2. **`rawRes.write(JSON.stringify(...))` 绕过响应格式检查** —— 属实。
   扫描器只认 `.end(`，换个方法名就漏。
3. **导出的两块空白** —— 属实。前 9 项的用例**全都只有 1 票货、全都走整柜模板**，
   分页那条路和客户签收模板一次都没跑过。

**而且这轮我又自己假绿了两次**（详见第 6 节）——所以这轮同样请你**默认我改错了**。

**基线（你上轮看的终点）**：`55cfa31`
**这一轮**：`2826e02` → `bb3f5b3` → `50fefcb` → `017ff19` → `c465939`
**范围**：21 个文件、+1340 / −84。**没有动数据库、没有迁移、没有改任何数据。**

```
git log --oneline 55cfa31..HEAD
git diff 55cfa31..HEAD
```

---

## ⛔ 不许做的

1. **不许部署、不许推送、不许跑迁移、不许写任何数据。** 结束时 `git status` 要跟开始一致
   （应当只剩原来那 4 张未跟踪图片）。
2. **不许在页面上做会改数据的操作。**
3. **不许调真的 DeepSeek**（要花钱）。
4. **不许改这几个已拍板的口径**：
   - 「今天/本月」按北京时间；「这个月发了多少货」= 这个月**下的单**（`createdAt`）
   - 统计类和查单类回答**不交给模型润色**，服务问答照旧润色
   - 登录限流**30 分钟 20 次**（老板 2026-08-29 定的，不要建议改这个数）
5. **不许为了让测试变绿去放宽断言。**
6. **`/images/*` 无认证这条已被老板拍板不修，不要报。**
   他的理由：想拿图的人本来就在页面上看着图，「另存为」比存网址省事，
   挡网址对内部人等于零。这个判断我同意。

---

## 一、产品数量：三条非法输入 + 前端把箱数猜成 1（`2826e02`）

你上轮真调 `/staff/orders` 拦写库参数测出来的三条，我全复现了：
每箱 `0` + 订单总数 `999` 存成 999；每箱 `-3` 放行；每箱 `1.5` × 2 箱存成 3。
另外你指出的**前端三处 `Number(...) || 1`** 也确认还在，
所以老板最早报的「箱数被系统猜成 1」改了后端也没解决。

改法：校验抽成纯函数，后端 `apps/api/src/modules/orders/product-row-guard.ts`、
前端 `apps/web/src/modules/orders/productRowGuard.ts`，**两份实现一份口径**；
三个前端页面（staff / admin / client）提交前先校验，兜底 `|| 1` 全部去掉。

**请你重点挑：**
1. **两份实现会不会漂移。** 我写了一项「前后端口径一致」的对拍，但用例只有 6 组。
   有没有哪类输入两边结论不一样（空串、`null`、全角数字、超大数、`"1e3"`、前后空格）？
2. **前端去掉 `|| 1` 之后有没有把能用的挡了。** 请把三个页面里
   **所有**读 `packageCount` 的地方过一遍 —— 我只改了「发送前」和「算合计」那几处，
   有没有别的地方（打印、导出、列表显示）现在会拿到 `NaN` 或 `0` 而显示成空白/0？
3. **订单级 `productQuantity` 我允许 0**（当「没填」）。这个口径对吗？
   有没有哪条路会因此把「真的 0 个」和「没填」混起来？
4. 建单接口里 `productQuantityFromRows > 0 ? 明细 : 前端传的` 这个兜底，
   加了正整数校验之后**还有必要吗**？会不会有输入让它走进错误的分支？

## 二、锁序三条 + 顺手五条（`bb3f5b3`）

你报的三条我都读代码确认了：
- 撤销柜子状态：父单锁**没排序**，而推进那条排了（同文件 ~490 行），同一个规矩两种写法；
  而且这条路**一把运单锁都没有**，直接 `updateMany`。
- 建派送单：`shipmentIds` 不排序，且**父单锁夹在两个子单锁中间** ——
  本事务拿着父单 P 要子单 S2，柜子推进拿着 S2 要 P，**成环**。
- 签收 `/admin/lastmile/status`：整条路没有任何锁，第二个人的 UPDATE
  等第一个提交后照样往下走 → 重复轨迹 + 签收图被盖。

顺手把 `KNOWN_UNFIXED` 里另外两条也修了（`/admin/orders/delete`、
`/client/prealerts/delete`），并且**新写的检查自己揪出两条你没提的**：
卸柜 `remove-shipment` 改子单却没锁子单；两条删订单路径删派送单行时没锁它。

全局锁序定为 **【订单 → 柜 → 柜内记录 → 派送单 → 运单 → 父单】**。

**请你重点挑（这一节最要紧）：**
1. **这个全局顺序本身对不对**，有没有哪条路径天然就不可能按它来。
2. **签收那条的「已签收就不再动运单」是不是改过头了。**
   现在重复签收只更新派送单本身（允许重新上传更清楚的签收图），
   但不再写轨迹、不再改运单。有没有正当场景因此做不成事了？
3. **删订单那两条**：我把锁序改成【订单 → 派送单 → 运单】，
   理由是签收那条是【派送单 → 运单】，反过来会成环。这个推理对吗？
   另外我在锁后**重读**了运单清单（原来用的是事务外那份）——
   重读之后其余代码还在用旧变量吗？
4. **有没有我新加的锁反而引入了新的环。** 请把所有会同时碰
   `shipments` + `admin_lastmile_orders` 的路径列出来对一遍。
5. 我**没有**做任何真实并发测试（要连库开两个事务，本地跑不动）。
   如果你能构造一个真实死锁场景，请直接给出可复现的两个事务的 SQL 顺序。

## 三、仓库版「已用方数」（`50fefcb`）

你报的 P2。锁内重算原来整段包在 `if (planTotal > 0)` 里，没设上限时返回事务外旧快照。
改成**无条件重算**，只有「拦不拦」还看 `planTotal > 0`。

**请你重点挑：** 无条件多跑一次 `sumPlanUsedVolume` + 一次 `findMany`，
在锁里多占时间。柜子里预报单多的时候，这会不会把锁持有时间拉长到影响并发？
有没有更省的写法（比如只在返回值真的会被用到时才算）？

## 四、测试守卫：你报的 4 处假绿，全部补实

### 4.1 锁序扫描器（原来 5 项 → 现在 7 项）
你说它「只能当提示」。三条假绿我都实测复现了，补了三处：
- `WRITE_RE` 只认 `tx.模型.方法` → 现在 raw SQL 的
  `INSERT INTO` / `UPDATE 表 SET` / `DELETE FROM` 也算写
  （注意别把 `SELECT ... FOR UPDATE` 当成写）
- **新增第 6 项**：取锁的循环，迭代的表达式里必须看得见 `.sort(`。
  **不接受**「SQL 里 orderBy 过所以有序」—— 那种下一个改代码的人看不见
- **新增第 7 项**：改了 `shipments`/`containers`/`orders`/`admin_lastmile_orders`
  就必须先锁住同一张表 —— 前面几项只查「已有的锁排得对不对」，
  不查「**该有的锁在不在**」，删掉一把锁反而没人管

`KNOWN_UNFIXED` 现在是空的。`WRITE_WITHOUT_LOCK_OK` 里只有一条
（建派送单的纯 `create`，那行还不存在、锁不到）。

**请你重点挑：**
1. **第 7 项的 `HOT_TABLES` 只有 4 张表**，别的表（`shipment_container_items`、
   `whr_*`、`consolidation_*`、`wallets`）不查。这个取舍对吗？漏了哪张真该查的？
2. **第 6 项只认 `.sort(`**。有没有正当写法会被误报？有没有加了 `.sort()`
   但实际没排序的写法能骗过它（比如 `.sort()` 之后又 `.reverse()`、
   或者对对象数组 `.sort()` 但比较的不是锁用的那个字段）？
3. **第 7 项是逐行扫的**，用 `held` 集合记「到这一行为止拿到了哪些锁」。
   多层嵌套、提前 `return`、`try/catch`、把锁包在 helper 里 —— 这几种它认不认得出？
4. **`WRITE_WITHOUT_LOCK_OK` 这条豁免站得住吗**（纯 create 真的不需要锁？）

### 4.2 响应格式（6 项，正则补 write/send + 自检）
### 4.3 AI 兜底分支不润色（62 → 63 项）
删掉兜底分支的 `answerHasBusinessData = true`，62×2 全绿 —— 因为
57~62 项测的都是别的分支。新增第 63 项，注释里写清了怎么走到兜底分支。
### 4.4 导出（9 → 13 项）
补了「26 票分两页 + 序号接着排」「正好 25 票不许多开一页」
「客户签收中文页明细与三个合计」「泰文页要清掉预置的 1..20 序号」。

**请你重点挑 4.4：** 我只测了 26 和 25 两个数。
51 票（三页）、0 票、以及**客户签收单按站点分组分页**（`paginateCustomerLines`，
每页 10 行、先按站点分组）那条路我**一项都没测**。请你直接去构造。

## 五、登录限流：补一道按账号算的（`c465939`）

你之前报过「换 IP 就能对同一账号无限猜」。老板拍板 **30 分钟 20 次**。
原有按 IP 那道保留不动。只数猜错的；登录成功清零；
**三个失败出口都记**（账号不存在/被封、role 对不上、密码错）。

**请你重点挑：**
1. **有没有第四个失败出口我漏了**（比如 body 解析失败、账号为空那条）。
   漏一条就等于给了一条不计数的试探通道。
2. **账号键统一小写**，但查库 `findUnique({where:{id: body.account}})` 是**区分大小写**的。
   也就是说计数比登录本身更宽 —— 这个不对称有没有可利用之处？
3. **DoS**：知道某人唛头的人故意连错 20 次把他挡半小时。老板认了这个代价。
   但请确认没有更严重的变体（比如能不能用它把**管理员**长期挡在外面）。
4. 计数在**进程内存**里，重启清零、多实例各算各的。这是已知前提，不用报。

## 六、⚠️ 这轮我自己假绿了两次（请重点验这两处的修法）

### 6.1 新写的第 6 项，正则第一版是 `of\s+([^)]+)\)`
迭代表达式里只要有一对括号（`[...ids].sort()` 就是）就匹配不上 ——
**一个循环都没扫到，7 项照样全绿**。我自己新写的检查自己又假绿了。
现在用贪婪 `(.+)`，并加了「至少扫到 6 个」的自检。
**请你验这个自检的阈值 6 是不是拍脑袋的，会不会以后加接口就失效。**

### 6.2 登录限流测试第一版**自己抄了一遍拼键的算法**
做变异把生产那句改成把 IP 拼进键里（**正是要修的病根**），**10 项照样全绿**,
因为测试比对的是它自己抄的那份。
现在抽成 `loginFailureKey()`，生产和测试共用，并加了一项读登录接口源码
确认它没绕过这个函数自己现拼。
**请你在整个仓库里找还有没有别的「测试重写一遍被测逻辑」** ——
这个项目里已经栽第三次了，我怀疑不止这两处。

---

## 七、我做过的验证（供你判断哪些不用重做）

- 10 个自测脚本、**110 项**，退出码全 0，全部已接进 CI
- 三套 TypeScript 检查退出码 0；Web 构建 25/25 页
- **变异验证做了 19 个**，全部会红：
  删逐票运单锁 / 父单锁反序（两处）/ 逐票运单锁反序 / 建派送单运单锁反序 /
  第一把锁前塞 raw UPDATE / `.write` 绕过 / 删 AI 兜底不润色标记 /
  分页 25→1000 / 第一页后不累加序号 / 克隆表不追加 / 客户页不写运单号 /
  不写方数合计公式 / 泰文页不清序号 / 后端删「每箱几个」校验 / 前端加回 `|| 1` /
  前端放行 0 / 限流键混进 IP / 登录接口绕过共用键函数 / 键不统一大小写 /
  三个失败出口漏记 / 成功不清零 / 查时顺手加计数 / 上限改 100 / `>=` 改 `>`

**我没做的：**
- 任何真实并发测试（要连库）
- 任何真实 DeepSeek 调用
- 978 单大客户的 AI 耗时压测
- 导出：三页以上、0 票、客户签收单按站点分组分页

# 请复核（第五轮）：4 个 P1 + 2 个 P2 全修完，请当我改错了来查

**先说清楚为什么这轮要请你查得比上一轮更狠。**

上一轮你报的 4 个 P1 里：
- **1 个是我上一轮只修了一半**（数字闸只焊住整行标签，普通句子没管）；
- **2 个是我上一轮自己改出来的新问题**（表头改名引入「两列并存看列顺序」，
  而且新表头「单箱数量」里含有「箱数」，直接把箱数那一列认错）；
- 剩下 1 个是我锁加对了方向、但**还在用锁前的旧数据**（CLAUDE.md 第 28 条的老教训，我又犯了）。

也就是说：**我上一轮的自我评估是不可靠的，四条里三条跟我自己的改动有关。**
所以这一轮请你**默认我改错了**，让我来证明我是对的，而不是反过来。

**基线（你上轮看的终点）**：`d21339b`
**这一轮**：`3fe29e8` → `1b6bac2` → `4107d86` → `5638899` → `5548048` → `e64b6ba` → `0496a67`
**范围**：18 个文件、+703 / −105。**没有动数据库、没有迁移、没有改任何数据。**

```
git log --oneline d21339b..HEAD
git diff d21339b..HEAD
```

---

## ⛔ 不许做的

1. **不许部署、不许推送、不许跑迁移、不许写任何数据。** 结束时 `git status` 与开始一致
   （应当只剩原来那 4 张未跟踪图片）。
2. **不许在页面上做会改数据的操作**：不点保存/提交/删除/撤销/报价/装柜/签收/付款。
3. **不许调真的 DeepSeek**（要花钱）。
4. **不许改这两个已拍板的口径**：「今天/本月」按北京时间；
   「这个月发了多少货」= 这个月**下的单**（`createdAt`）。
5. **不许把「宁可提示查不到，也不报错的数」反过来。** 报小可以解释，报大会让客户以为货多了。
6. **不许为了让测试变绿去放宽断言。**

---

## 一、这一轮改了什么（按你上轮报的编号）

### P1-1 数字闸仍可改义 → 改成「带数据的那一行，一个字都不许改」

本地复刻了你那个攻击，输出跟你说的一样：
「你当前一共查到 ⟦N1⟧。」→「你当前**已完成** ⟦N1⟧。」→ 客户看到「已完成 3 单」，实际只完成 1 单。

我上一版只把「标签：数字」这种**整行**焊进占位符，句子里的说明文字管不到。
列举「哪些词不许改」堵不完（一共/已完成/在途/共计/总共…），所以换成白名单：
**含占位符的行必须和草稿逐字一致**（只忽略行首尾空白）。
模型只能在**不含数据的行**上加话。

⚠️ **请你重点挑这条的副作用**：
1. **这是不是把润色变成摆设了？** 真实模型十有八九会顺手改一下数据那句话的措辞
   （「你当前一共查到 3 单」→「您目前共有 3 票在库」），那样每一次润色都会被退回、
   客户永远只看到模板腔的原始草稿。
   **如果是这样，这个修法虽然安全但产品上是失败的，请直接说。**
   我没法验——不连真 DeepSeek。这是我最没底的一条。
2. 我只忽略了**行首尾**空白。模型在数据行**中间**多打一个空格（`总单量： 3 单`）就会被退回。
   这算不算太严？
3. 新加的测试是第 58/59/60 项，请看我有没有把「正常润色」的正向用例写得太宽松。

### P1-2 数量口径四个缺口 → 全补，另修掉我自己引入的两个

| # | 问题 | 谁造成的 |
|---|---|---|
| 1 | 新旧两列并存时**取值看列顺序**（同数据能算出 10 或 200） | **我上一版引入的** |
| 2 | 新表头「单箱**数量**」里含「**箱数**」→ 箱数那列被认成数量列（5×7 算成 49） | **我上一版引入的**，我自己的新测试抓到 |
| 3 | 管理员手动改单也漏乘箱数（admin/page.tsx:577） | 原有 |
| 4 | 后端前端没传就存 0；部分行没填按 0 静默计入 | 原有 |

表头最终定成「**每箱几个**」，理由是它跟现有任何表头都不含相同子串。
新旧两列都认、**新列固定优先**（分两次查，不靠列顺序）。
部分填写现在报错（跟单箱重量、尺寸同一个规矩）。

⚠️ **请你重点挑**：
1. **「每箱几个」这个名字真的不跟任何表头撞吗？** `findValue` 是
   `candidate.includes(keyword)` 的包含匹配，方向是**表头包含关键词**。
   请把所有关键词和所有模板表头做一次**双向**交叉检查 ——
   我只检查了「每箱几个」不含别的关键词，**没系统地检查反方向**。
   这正是我上一版翻车的地方，我很可能又漏了。
2. **「要么全填要么全空」会不会拦住现在能正常导入的文件。** 这是行为收紧，
   历史上员工可能就是只填几行。我认为拦住比算错强，但代价是他们的工作流被打断。
3. **后端那个兜底**（前端没传就按 Σ(箱数×单箱数量) 算）会不会跟员工在订单级手填的总数打架。
4. **管理员改单那处** `(Number(p.packageCount) || 1)` —— 箱数为 0 或空时我兜底成 1，
   这个兜底对不对？会不会把「没填箱数」算成 1 箱。

### P1-3 柜子并发 → 锁完必须重读

三处：
- **推进柜子状态**：装柜清单和运单状态原来是**事务外**读的，锁完照旧用旧清单 →
  锁后新装进来的那票不会被推、不写轨迹，永远停在旧状态。
  改成锁后重查 `shipmentContainerItem` 和运单状态；状态流转合法性也在锁里重判；
  返回给前端的 `affectedShipmentCount` 改成**真的推了多少票**。
- **装柜**：柜子信息虽在事务里读，但**读在锁之前**，而它要拿柜子状态给新货**补历史轨迹** →
  改成先锁再读。
- **封柜**：**原来完全没有事务、没有锁**，能把已开船的柜子改回 SEALED。补上锁 + 重查。

⚠️ **请你重点挑**：
1. **推进那处是结构性改动**（把攒 `ops` 函数数组那套拆了、代码整块搬进事务）。
   我按 CLAUDE.md 第 30 条逐行看过 diff，第一版漏了个右大括号（类型检查报出来了）。
   **请你再逐行核一遍语义有没有被搬错** —— 尤其：
   - 轨迹 id 前缀是不是还是 `sl_ctn_`（撤销柜子状态靠它认记录，红线 2.10）；
   - `fromStatus` 用的是不是锁后的状态；
   - `parentNosToSync` 是不是从**锁后**的运单里收集的；
   - 30 秒超时和 maxWait 还在不在。
2. **有没有引入新的死锁。** 我现在的锁序是【柜 → 运单 →（父单，按单号排序）】。
   请把所有会同时碰 containers / shipments / shipment_container_items 的路径
   列一张锁序表核对。**我只核对了卸柜那一条。**
3. **事务变长了**（多了两次查询在锁里）。一个柜几十票货时会不会撞上超时？

### P1-4 集货锁序 → 统一【任务 → 预报单】，合计一律在锁里重算

签收、客户改单、管理员强改、创建预报单这四条原来没跟上：
客户改单只锁预报单却又写任务合计（跟删除那三条**方向相反**，会死锁）；
签收和创建干脆在事务外重算合计（删除那边刚删掉的货会被写回来）。

我写脚本扫了这个文件里每个 `$transaction` 块的 `FOR UPDATE` 顺序：
**17 条路径全部是【任务 →（预报单）】，7 个 `recalcTaskTotals` 全在事务里、全握着任务锁。**

⚠️ **请你重点挑**：
1. **我那个扫描脚本本身可靠吗？** 它是按行正则找 `FOR UPDATE` 和 `$transaction`，
   **认不出通过函数间接加的锁**（比如某个 helper 里也有 FOR UPDATE）。
   请你用别的办法核一遍，别信我的脚本。
2. **签收那条我只把「重算」包进事务，`updateMany` 没挪进去**，
   理由是它带 `status: "pending"` 条件、本身是原子的。这个判断对不对？
3. **锁的粒度会不会太粗**：现在创建一张预报单也要锁整个任务行，
   同一个任务下多个客户同时报单会不会排队排太久。

### ⑤ 顺带修的：清理旧图前先确认没人引用

你归到「次要」，但它是**丢数据**（文件被删、数据库还引用着、客户看到裂图且找不回），
所以我一起修了：每删一个之前查一次全表还有没有人引用。

⚠️ 请挑：这仍然有一个很窄的窗口（查完到 unlink 之间）。要不要更彻底的做法
（比如软删除 / 定时清理孤儿文件）？

### P2-1 导出测试补到最终 XML 层

原来只验 `expandTemplateLines` 的中间对象。现在把
`patchInternalTemplate` / `SharedStringsEditor` 导出，喂一张最小工作表 XML，
直接读**生成出来的单元格 XML**。5 → 9 项。

**变异验证**：
- 字符串塞进数字格 → 第 7 项红
- 数字走文本格 → 第 6 项红
- **而这两次变异下，原来那 5 项中间对象测试全是绿的** —— 正好证明了你说的「容易假绿」。

⚠️ 请挑：
1. 我喂的是**自己造的最小工作表 XML**，不是真模板。
   真模板里格子可能带 `s="12"` 这类样式属性、可能是 `<c r="G10" t="s">` 已有类型 ——
   我的 fixture 可能过于干净，测不出真模板上的问题。**你用真模板验一遍。**
2. 我导出了两个原本私有的东西（`patchInternalTemplate` / `SharedStringsEditor`）
   只为了测试。这算不算为了测试破坏封装？有没有更好的做法？

### P2-2 错误响应统一 + requestId

自己拼响应体的有三处：全局 BusinessError、500 兜底、404 路由不存在。全改成走 `fail()`。
`requestId` 真正接通（共享类型里 `requestId?` 早就声明了，从来没人填），
并写进 `X-Request-Id` 响应头。

新测试第 4 项会**扫全后端源码**，逮住任何绕开 `ok()/fail()` 自己拼响应体的地方。
变异验证：改回自己拼 → 当场红并报出行号。

⚠️ **请你重点挑（这条动的是全系统的响应格式，风险最大）**：
1. **前端会不会被打到。** 我判断是纯新增字段、`parseApiResponse` 读的是 `data`/`message`，
   所以不受影响 —— 但这是**读代码推的**，我没在页面上验。
   请你把所有读响应体的地方过一遍（含 `errors` 字段的消费方、登录那条特殊 401 分支）。
2. **500 现在也走 `fail()`**，而 `fail` 会把 message 放进 `errors[0].reason`。
   生产环境 message 是「Internal server error」，不泄漏细节；
   **但非生产环境会把异常原文放进去** —— 这算不算把内部信息多暴露了一个位置？
3. **`X-Request-Id` 响应头**会不会跟 nginx 或前端已有的什么东西冲突。
4. 我的扫描是**正则找 `.json({` 附近有没有 `code:`**。
   请挑：有没有写法能绕过它（比如先构造对象再 `res.json(payload)`）。

---

## 二、我真跑过什么

| 命令 | 结果 |
|---|---|
| `npm run test:ai-numbers` | **60 项 × 2 时区**全过 |
| `npm run test:batch-import` | 100 单 / 300 行全过 |
| `npm run test:lastmile-export` | **9 项**（含 4 项 XML 层）全过 |
| `npm run test:consolidation-delete` | 7 项全过 |
| `npm run test:api-response` | **4 项**（新）全过 |
| `npm run test:ai-limits` / `test:ai-timeout` | 9 项 / 3 项全过 |
| 三套 `tsc --noEmit` | 退出码全 0 |
| `npm run build` | 25/25 页 |

新增的两个测试脚本都接进了 CI，CI 现在 7 个测试步骤。

**每一条修改我都做了变异验证**（把源码改回坏的样子，确认对应测试会红）。
但**变异是我自己设计的** —— 上一轮我就有过一次变异写错、看起来像抓得住其实没抓住。
**请你对我这一轮新增的所有测试也做一遍变异，用你自己设计的变异。**

---

## 三、我明确没验证的（请优先看）

1. **P1-3、P1-4、⑤ 三条并发修改没有任何自动化测试** ——
   要连数据库开两个并发事务，本地测不动。根因和改法都是读代码核对的。
2. **P1-1 的产品后果没验**：不连真 DeepSeek，不知道「数据行一个字都不许改」
   会不会让润色实际上永远不生效。
3. **P2-2 没在页面上验**：前端不受影响是读代码推的。
4. **锁序一致性是脚本静态扫出来的**，不是实测，而且脚本认不出间接加的锁。
5. 上一轮遗留：978 单大客户没压测；限流计数在进程内存里、重启或多实例会重置。

---

## 四、汇报要求

1. **写清楚哪些是你实测的、哪些是读代码推的。** 老板被误报坑过，非常在意这个区分。
2. **优先级**：先看「我改的东西有没有引入新问题」，再看「有没有没修干净」，
   最后才是找新 bug。前两类比第三类要紧得多 ——
   上一轮四条里有三条跟我自己的改动有关。
3. 每条给：**哪个文件哪一行、什么操作、会出什么后果**。
4. 对我新增的测试做**你自己设计的变异**，看它们是不是真的抓得住。
5. 最后回答一句：**这个分支现在能不能上线？** 不能的话还缺哪几条。
   如果你认为 P1-1 那个修法在产品上不可接受（润色被彻底废掉），请直接说，
   我宁可回头重新设计，也不想上线一个「安全但没人用得舒服」的东西。

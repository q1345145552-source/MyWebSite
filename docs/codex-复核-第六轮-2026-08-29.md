# 请复核（第六轮）：5 组 P1 + 2 组 P2 全修完

**你上一轮的三条判断，事后都被证明是对的，而我错了：**

1. **AI 数字闸**：你说我的修法两头不讨好（正常润色被退回、模型加句假话却放行）。
   老板看完你的实测直接拍板：**统计和查单类回答不再交给 DeepSeek 润色**。
2. **导出测试假绿**：你说我那个最小 XML fixture 太干净。确实 ——
   我上一版代码里**注释写着「长宽高不做合计」，实际一行没动**，
   模板自带的 `G35=SUM(G10:G34)` 原样留着，多尺寸时印出三个 0。
3. **响应格式扫描能绕过**：你把响应体先存进变量就绕过去了。确实。

另外**我自己新写的锁序测试，被我自己写的注释骗了**（详见第 6 节）——
所以这轮同样请你**默认我改错了**。

**基线（你上轮看的终点）**：`dc2be2b`
**这一轮**：`1f65870` → `490830b` → `bca6541` → `2b263bd` → `f74ca65`
**范围**：15 个文件、+842 / −211。**没有动数据库、没有迁移、没有改任何数据。**

```
git log --oneline dc2be2b..HEAD
git diff dc2be2b..HEAD
```

---

## ⛔ 不许做的

1. **不许部署、不许推送、不许跑迁移、不许写任何数据。** 结束时 `git status` 与开始一致
   （应当只剩原来那 4 张未跟踪图片）。
2. **不许在页面上做会改数据的操作。**
3. **不许调真的 DeepSeek**（要花钱）。
4. **不许改这两个已拍板的口径**：「今天/本月」按北京时间；
   「这个月发了多少货」= 这个月**下的单**（`createdAt`）。
5. **不许把「宁可提示查不到，也不报错的数」反过来。**
6. **不许为了让测试变绿去放宽断言。**

---

## 一、P1-1 AI 改义 → 统计类回答**不再润色**（`1f65870`）

老板 2026-08-28 拍板。带**系统自己算出来的业务数据**的答复
（统计、查单进度、查无此单、兜底汇总）一律直接发系统写好的原话，**连模型都不调**。
服务问答（时效、清关、能不能寄…）照旧润色 —— 那才是润色有用的地方。

好处：假话不可能出现（模型没参与）；这个接口是全系统唯一直接花钱的地方，
这类消息少调一次 DeepSeek；客户看到的东西可复现。
代价：统计回答固定是模板腔 —— 老板认了。

⚠️ **我第一版按「有没有数字」判，结果把润色整个废掉了** ——
服务问答里那句「已投喂的业务知识（当前 **0** 条）」也带数字。
改成**按分支标记**（`answerHasBusinessData`）。

**请你重点挑：**
1. **分支标记有没有漏。** 我在四处设了 `true`（查单命中/未命中、统计分支、兜底分支）。
   请把 `chat()` 的**每一条**出口过一遍，看有没有哪条会带系统算的数字、却还在走润色。
   尤其：服务问答的答复里如果引用了知识库中的数字（比如「45 天」），
   现在仍然会被润色，模型改掉它算不算问题？
2. **反过来有没有误伤**：有没有本该润色的答复被当成「带数据」而不再润色了？
3. 测试 60 → 62 项，其中**改写了 4 项**（它们断言的是被砍掉的旧行为）。
   请确认我改写的时候**没有顺手放宽断言**。

## 二、P1-2 数量口径（`2b263bd`）

你报的三条都在 orders 建单接口这一处：
· 空箱数被 `Math.max(1, p.packageCount || 1)` 悄悄当成 1 箱 → 改成必须是正整数；
· 「每箱几个」只填几行时其余按 0 计入 → 补「要么全填、要么全空」；
· 前端传的订单总数无条件盖过明细合计 → 改成**有产品行时以产品行为准**。

**请你重点挑：**
1. **「有产品行就以明细为准」会不会跟员工的实际用法打架** ——
   员工在订单级手填的那个「产品数量」现在只有「没有产品行」时才生效。
   这算不算把一个他们在用的功能悄悄关掉了？
2. 箱数必填这道闸：前端三端都是 `Number(p.packageCount) || 1` 才发出来的，
   所以正常操作行为不变。**请确认三端确实都是这样**，别有哪个端会发 0 或不发。
3. 「要么全填要么全空」在 API 层和批量导入层各写了一遍，两处口径一致吗？

## 三、P2-1 导出（`490830b`）

· **注释说清掉、代码一行没动** → 现在真的 `clearRange` 清掉 G35/H35/I35；
  E35/F35（方数、重量）的合计必须保留。
· 测试改成走**完整链路**：读真 xlsx → `buildLastmileTemplateWorkbook` → 解压读 XML。
  新增第 8 项专盯**样式属性**有没有被写值那一步吃掉（就是你那个变异）。
· **撤掉了上一版为测试而导出的两个私有类**（`patchInternalTemplate` / `SharedStringsEditor`）。

变异验证：删掉样式属性 → 第 8 项红（上一版全绿）；不清合计格 → 第 9 项红。

**请你重点挑：**
1. 清掉 G35/H35/I35 会不会让模板里的**合并单元格、边框、打印区域**出问题
   （clearRange 是用 setTextCell 写空串实现的）。请用 LibreOffice 再打开看一眼版式。
2. 客户签收模板（`customer-receipt-template.xlsx`）那条路我**没动也没测**，
   它有没有同类问题？
3. 我的 fixture 里 `scope: "container"`、一票货、28 行以内。
   **超过 25 行会走克隆工作表那条路**（`paginate` + `appendWorksheetClones`），
   那条路我一项都没测。请你重点看它。

## 四、P2-2 响应格式（`bca6541`）

不再要求「大家都去调 `ok()/fail()`」（靠人记得，记不住），
改成**在唯一出口 `createJsonResponse.json()` 上盖章**：
补 `requestId` / `timestamp`，只认带 `code` 的对象，已有值不覆盖。
**结果：AI 那 52 处调用一行没改，requestId 全盖上了**（实跑确认过）。
管线兜底那条绕开了 `json()`，自己补齐了全部字段。

测试从「正则扫源码」改成**真的跑一遍**（4 → 6 项），
包含你那个「先存变量再发」的绕过写法。

**请你重点挑：**
1. **盖章会不会误伤**。我只认「对象 + 有字符串 code 字段」。
   静态图片、下载文件、SSE 之类的响应会不会碰巧长这样？
   有没有哪个接口的 `data` 里本来就有个叫 `code` 的业务字段
   （比如错误码列表、国家代码），被我当成响应体信封改了？
2. **`X-Request-Id` 响应头**：nginx、CDN、前端有没有已经在用同名头。
3. 我第 6 项只扫 `.end(JSON.stringify(`。还有别的绕开 `res.json` 的写法吗
   （比如 `res.write` 分段写、或者拿到 rawRes 直接操作）？

## 五、P1-3 / P1-4 / P1-5 并发（`f74ca65`）⚠️ 没有自动化测试

**集货**：管理员强改的锁提到事务最开头（原来是产品→任务→预报单）；
创建预报单改成锁内重查任务状态再插；签收整段进锁。

**柜子**：推进柜子补上**按 id 排序逐个锁运单**；卸柜先锁柜；改运输方式加锁重判；
封柜从黑名单改成**白名单（只有 LOADING 能封）**；
另外自己发现「删轨迹」会按剩余轨迹改运单状态却不锁运单，也补了。

**仓库版**：返回的已用方数改用锁内算的；图片白名单在锁内重验
（防止后一个人保留一个前一个人刚拿掉的路径）。

**请你重点挑：**
1. **有没有引入新的死锁。** 推进柜子现在要按顺序锁**一整柜的运单**，
   一个柜几十票货时事务会明显变长（30 秒超时够不够？）；
   而且它同时还要锁父单。请把「柜 → 柜内记录 → 运单 → 父单」这条链
   跟所有别的路径交叉核一遍。
2. **封柜改白名单会不会挡掉正常操作** —— 有没有合法场景是从别的状态封柜的？
3. **签收整段进锁**之后，仓库签收会不会跟客户付款、员工报价互相排队排太久？
4. **图片白名单锁内重验**：我抛的是 409「你保留的那张已经不在了」。
   这个错误信息对客户来说够不够懂？会不会误伤正常的重复提交？
5. P1-5 那个「查引用 → unlink」的窗口我**没有彻底关掉**，
   只是把白名单验证挪进了锁里。你上轮建议的「后台延迟清理」我没做
   （要加定时任务，得老板拍板）。请评估现在这个状态可不可接受。

## 六、⚠️ 新增的锁序测试，以及它扫出来的东西（`f74ca65`）

你说「正则扫描会误判为锁序统一」是对的。新测试**不再扫锁在哪**，
而是比**第一条写语句和第一把锁的行号** —— 锁在写后面等于白加。5 项，已接进 CI。

**⚠️ 这个测试第一版被我自己的注释骗了。**
我在源码里写的那句「以后判断锁序不能只看 **FOR UPDATE** 的位置」含有那四个字，
被扫描器当成一把真锁 —— 把加锁语句删掉做变异，测试照样全绿。
跳过注释行之后才抓得住。**跟它要防的是同一类毛病。**

**⚠️ 它还扫出三条不在你复核范围内、确实没加锁的路径**，我**没有偷偷放行**：
`/admin/orders/delete`、`/admin/lastmile/status`、`/admin/lastmile/orders`。
列在 `KNOWN_UNFIXED` 里，另加第 5 项盯着「这张表只许变短不许变长」，等老板拍板再改。

**请你重点挑：**
1. **这个静态检查本身可靠吗？** 它认不出更多间接加锁的写法（我只映射了 plan-guard 那 4 个 helper）。
   请你找出**它会误判的场景** —— 漏报（该红没红）比误报严重得多。
2. `KNOWN_UNFIXED` 那三条你评估一下**有多急**，值不值得这一轮就修。
3. 第 4 项那个「事务数量不能少于 30」是个很粗的护栏（现在扫到 50 个）。有更好的写法吗？

---

## 七、我真跑过什么

| 命令 | 结果 |
|---|---|
| `npm run test:ai-numbers` | **62 项 × 2 时区**全过 |
| `npm run test:batch-import` | 100 单 / 300 行全过 |
| `npm run test:lastmile-export` | **9 项**（其中 4 项走真模板）全过 |
| `npm run test:consolidation-delete` | 7 项全过 |
| `npm run test:api-response` | **6 项**全过 |
| `npm run test:lock-order` | **5 项**（扫了 50 个会写数据的事务）全过 |
| `npm run test:ai-limits` / `test:ai-timeout` | 9 项 / 3 项全过 |
| 三套 `tsc --noEmit` | 退出码全 0 |
| `npm run build` | 25/25 页 |

CI 现在 9 个测试步骤。**每条改动我都做了变异验证**，但**变异是我自己设计的** ——
上一轮你用自己设计的变异抓出了我三组假绿。
**请你继续对我这一轮新增/改写的测试做你自己的变异。**

---

## 八、我明确没验证的

1. **并发那五条（集货三处、柜子五处、仓库版两处）没有任何自动化测试** ——
   要连数据库开两个并发事务，本地测不动。根因和改法都是读代码核对的。
2. **AI 那条没在真页面上验过**，也不知道客户会不会抱怨「回答变生硬了」。
3. **导出超过 25 行的克隆工作表那条路一项没测。**
4. **客户签收模板那条路没动也没测。**
5. 上一轮遗留：978 单大客户没压测；限流计数在进程内存里、重启或多实例会重置。

---

## 九、汇报要求

1. **写清楚哪些是你实测的、哪些是读代码推的。**
2. **优先级**：① 我这轮改的有没有引入新问题 → ② 有没有没修干净 → ③ 新 bug。
   前两类比第三类要紧。
3. 对我新增/改写的测试做**你自己设计的变异**。
4. 如果有哪条修法你认为方向就不对（像上轮 AI 那条），**直接说，我重新设计**。
5. 最后回答：**这个分支现在能不能上线？** 不能的话还缺哪几条。
   如果能上线但有前提（比如「先修 KNOWN_UNFIXED 那三条」），请把前提列清楚。

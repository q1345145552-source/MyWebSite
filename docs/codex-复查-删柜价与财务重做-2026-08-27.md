# 请复查这一批（2026-08-27）

这一批攒了三块改动，**没有部署**。请你复查代码，**并且替我完成第四节那两件我没法做的验证**。

---

## ⛔ 先说不许做的

1. **不许部署、不许推送、不许跑迁移、不许写生产数据。** 结束时 `git status` 必须和开始时一致（除非我在下面明确请你改）。
2. **不许改排版**（几列、列顺序、每行怎么排、列宽、加列减列）。
3. **不许改分柜算法、`split-metrics.ts`、`total-metrics.ts`。**
4. **不许改两个 xlsx 模板文件**（打印设置那个问题是模板自带的，已知，不在本轮范围）。
5. **不许把「柜价维护」加回来** —— 是老板明确要求删的。

---

## 一、这一批改了什么

### 1.1 【删除】柜价维护功能，整个删干净

老板判断这功能没用（填了价格系统不汇总、不进报表、不进导出、不算利润，纯记事本），要求删掉。

删的范围：
- 迁移 `20260825_add_container_lastmile_prices/` —— **整个目录删掉**（这个迁移**从没上过生产**，生产库里根本没有那张表，所以删了不会留孤儿表）
- `schema.prisma`：`ContainerLastmilePrice` 模型 + `Container.lastmilePrice` 关联
- `admin-ops/routes.ts`：`GET/POST /admin/lastmile/container-prices` 两个接口（106 行）
- `admin/page.tsx`：整块界面 —— 标签页、表格、状态、函数、类型（287 行）
- **连带删掉了尾端派送那一层标签栏本身**（只剩「派送作业」一个标签了，留着没意义）
- `globals.css`：31 条 `.lastmile-price*` 规则
- 测试库里那条迁移记录（生产库没有，不受影响）

**要你核实**：有没有删漏（残留引用）、有没有删多（把别的功能连累了）。

### 1.2 【重做】财务管理页 `/admin/finance`

老板口径：**运单跟钱无关，钱只在集货那两个功能里**（普通版 + 仓库版）。

原来这页按「每张运单」列重量体积和付款状态 —— 方向就跟生意不符（生产 1063 张订单里 0 张录过应收、0 张标过已付款，那页从上线到现在是空的）。

改成四个数字 + 一行一张集货单：
- 待收款 / 已收款 / 客户余额 / 未到收款环节
- 表格：类型 / 单号 / 客户唛头 / 状态（中文）/ 金额 / 付款 / 创建时间

文件：`apps/api/src/modules/finance/routes.ts`（整个重写）、
`apps/web/src/app/admin/finance/page.tsx`、`business-api.ts` 的 `FinanceRow`/`FinanceSummary`

### 1.3 【重做】柜子收款页 `/admin/settlement`（原「财务结算与利润」）

老板原话：**「不用算利润，就是这条柜收客户多少钱就行了」**。

原来要手工填「客户应收 − 供应商应付 − 税费 = 利润」，生产上结算表 0 行、从没人用过。
改成一行一个柜，能展开看柜里每个客户各付了多少。

「一个柜」的定义（我查代码和生产数据定的，请你核实这个判断对不对）：
- **普通版** = 一个 `ConsolidationTask`（`JH…`），`maxVolumeM3` 默认 68，一个任务一个客户
- **仓库版** = 一个 `WhrConsolidationPlan`（`WHR…`），有 `containerType`（40HQ 等），
  底下挂多个 `PlanCustomer`，每个客户挂多张 `Prealert`；钱按 预报单 → 客户 → 柜 逐层汇总

新接口：`GET /admin/settlement/by-container`（`admin-ops/routes.ts`）
⚠️ 旧的三个接口（`/admin/settlement/entries` ×2、`/profit`）**代码还留着但前端已无调用方**，本轮没删。
菜单名改成「柜子收款」。

---

## 二、⚠️ 特别要你核实的一件事：我误删过 `globals.css`

删柜价 CSS 的过程中，我先用正则删坏了规则（删了规则体留下选择器），
然后**误用 `git checkout -- globals.css` 想还原** —— 但那批 lastmile 样式当时还没提交，
结果**把 codex 之前加的 60+ 条尾端派送样式一起还原掉了**。

我的恢复方式：
```
.audit/2026-08-26/lastmile-format-simplification/original/apps/web/src/app/globals.css
  + 该目录下 lastmile-format-simplification.patch 里 globals.css 那一段
  + 重新做一遍进度条 CSS 清理（P3）
  + 重新删一遍柜价 CSS（这次整条删，删完检查了大括号配平）
```

**请你逐条核实恢复是否完整**：
- 除了「柜价那 31 条」和「进度条那 2 条 + 一个 `margin-bottom: 5px`」之外，
  **有没有任何一条尾端派送样式在这个过程中丢了**
- 有没有留下孤立选择器（没有规则体的选择器行）
- 建议做法：拿 `.audit/2026-08-26/lastmile-format-simplification/` 的 original + patch 重建一份
  期望结果，再跟当前文件逐规则比对，**明确列出差异**

这一条是本轮**最需要外部眼睛**的地方，因为出问题的是我自己。

---

## 三、我真跑过的（不用重复）

| 验证 | 结果 |
|---|---|
| `apps/web` 类型检查 | 0 |
| `apps/api` 类型检查 | 19（历史基线，不许多） |
| `apps/web` 构建 | 通过 |
| 浏览器实测（本地 + 测试库，管理员登录） | 尾端派送、财务管理、柜子收款三页都打开点过，**控制台 0 错误**；柜子收款的「客户明细」能展开能收起 |
| 财务页数字对账 | 页面显示 待收 ¥255.00(4张) / 已收 ¥0.00(0张) / 余额 ¥20,200.00，**我另写脚本手算一遍完全一致** |
| 柜价删除后的残留扫描 | 源码 0 处（剩余命中都在 `.next/` 构建缓存里） |
| 生产库只读核对 | 本地和服务器迁移目录已一致 → **本次上线不需要跑迁移** |

---

## 四、⭐ 必须替我完成的两件验证（我做不了）

老板点名要你做这两件。**这是本轮最重要的交付物，不是可选项。**

### 4.1 三个导出按钮 —— 用 Excel / WPS / LibreOffice **真打开看**

我只把 xlsx 拆开看了 XML 里的值和公式，**从来没有真打开看过排版**。请你：

1. 真触发这三个导出（不要只调生成器，要走真实按钮 → 真实接口 → 真实文件）：
   - 客户派送签收单（`导出该客户派送单`）
   - 整柜拆柜派送清单（装柜管理里）
2. 用 LibreOffice 之类真打开，**截图**，看：
   - 排版、边框、列宽有没有乱
   - `SUM` 公式打开后算不算得出来
   - **泰文那一页的字形、换行、有没有缺字**
   - 超过 10 行自动翻页后，第 2 页的表头和序号对不对
3. ⚠️ 已知问题**不用再报**：模板没设打印区域，打印会拆成额外窄页 —— 你上一轮已经验证过
   「空模板本身就有这毛病」，属于模板问题不是代码问题，本轮不修。

### 4.2 财务两页 —— 打开看数字对不对

请你在本地真登录，打开 `/admin/finance` 和 `/admin/settlement`，**截图**，并且：

1. **另写一份独立的核对脚本**（不要复用我的口径，自己从数据库直接算），
   对比页面上每一个数字
2. 重点核对这几条口径对不对：
   - **待收款** = 货已到仓、报了价、客户还没付（仓库版 `received_pending_payment`；
     普通版已报价未付）—— 我**没有**把仓库版 `pending`（货还没到仓）算进待收
   - **已收款** = 仓库版 `paid/loading/shipped/thailand_received`；普通版 `paymentStatus='paid'`
   - **已取消的单一律不进任何数字**
   - **没报价的金额显示「—」不显示 ¥0.00**
3. 柜子收款页：展开「客户明细」，核对**柜里各客户金额相加 = 柜的合计**

---

## 五、我没验的 / 已知不用报

- Excel 真打开的视觉效果（就是请你做的 4.1）
- 模板打印拆页（模板自带，本轮不修）
- 尾端派送界面是整个重做的，员工第一次看会不认识 —— 这是业务沟通问题，不是代码问题
- 旧的三个 settlement 接口留着没删（前端已无调用方），本轮有意不动

---

## 六、这个系统特有的坑（不知道会查错）

1. **三端各写各的**，改一处要确认另外两处。⚠️ 本轮已确认：**员工端「财务」底下只有「客户集货余额」**，跟改的两页无关。
2. **集货两个版本算钱方式不一样**：普通版全手填（订舱费+清关费+装柜费）；
   **仓库版是签收那一刻系统按「方数 × 单价」自动算**，整个模块没有手填总额的入口。
   老板 2026-08-26 拍板**保持现状不改**。
3. 状态判断要用**黑名单**不要用白名单（`CLAUDE.md` 第 13 条），这个库状态值散在各处、命名不统一
4. 物流轨迹写在**子单**上；客户看到的是父单 + 全部子单合并
5. 柜货关系在 `shipment_container_items`，不是 `shipments.batch_no`
6. 后端容器跑 **UTC**，「今天」要按北京时间换算
7. 海运 22 步 / 陆运 16 步两套流程，不许串
8. `apps/api` 的 tsc 基线就是 **19 个错**，别去修
9. 手写 SQL 的列名要去数据库核，这个库 `@map` 用得不统一
10. **红线：客户不能看到柜号、不能看到价格**
11. **本地后端不会自动重载**：验后端改动前必须 `lsof -ti tcp:3001 | xargs kill -9`，并核对进程启动时间晚于代码修改时间
12. 空值不要显示成 0，列表不要静默截断

---

## 七、汇报要求

**明确分开写「真跑过的」和「只读代码的」，不要混在一起说。**
截图和转换出来的文件请留在 `.audit/` 里并给出路径。

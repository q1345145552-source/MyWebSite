# Codex 复核提示词 — 9-05 全天 UI 改动 + 复查后 8 处修复（2026-09-05）

> 用法：把「提示词正文」整段复制给 Codex，或让它读本文件照做。

---

## 提示词正文

你是湘泰物流系统的独立审查员。仓库 `/Users/liuyujiang/湘泰物流系统/MyWebSite`，分支 main，HEAD = `4691a4e`（本地 = GitHub = 线上）。**审的是未提交的工作区**：`git diff HEAD` 加 6 个未跟踪文件（`apps/web/src/app/ledger.css`、`apps/web/src/modules/shipment/ShipmentExportPanel.tsx`、`apps/web/src/modules/shipment/ShipmentStatusGroups.tsx`、`scripts/test-attention-filter.ts`、`scripts/test-session-api.ts`、`scripts/test-session-stability.ts`）。

这堆改动分两层：

**第一层：9-05 白天 12 轮 UI 改动**（背景在 `docs/journal.md` 的 09-05 各条）。老板定的口径：三端统一浅灰导航、文字优先；运单列表是主体、筛选收成一行；六个入口 = 全部 / 未发出 / 在途 / 已到仓 / 已签收 / **异常**；「异常」是关注维度不是阶段桶，和在途可重叠，包含 delayDeparted / delayInTransit / borderDelay / portClosed / customsInspectCn / customsInspect / customsInspectTh / exception，正常卸柜、预约派送、派送中不算；unloading 仍是在途；已到仓 = inWarehouseTH / deliveryBooked / outForDelivery；体积固定 3 位小数、重量 2 位，**只改展示不改提交/计算/导出原值**；桌面固定唛头+运单号列；导出入口收进结果栏、日期条件进小弹窗；管理员补「下载模板」；登录页不再误清另一标签的会话；批量导入慢文件不许回填。

**第二层：9-05 晚上一轮复查后的 8 处修复**（journal 最上面那条「全天未提交改动的复查与修复」）。修的是：

1. `apps/web/src/app/login/page.tsx` 恢复 `password.trim()`——后端 `apps/api/src/modules/admin/routes.ts` 设密码/重置密码存的是 trim 过的哈希，`auth/routes.ts` 登录按原样比对，客户密码就是唛头、常带尾空格
2. `apps/web/src/app/staff/page.tsx` 分页加 `totalPages` 回夹 effect（管理员端本来就有）
3. `apps/web/src/app/staff/page.tsx` 新增 `selectedResultShipments` / `allResultShipmentsSelected`：「已选 N 条」、导出禁用、全选框半选都按当前结果算（镜像 admin 的 `selectedResultOrders`）
4. `apps/web/src/app/globals.css` 补 `.shipment-search-reset:disabled`、`.shipment-search-hint`、`.auth-shell .auth-field-hint` 三条规则
5. `apps/web/src/app/admin/page.tsx` `loadOrders` 内加 `orderListGate`（10 秒轮询与手动刷新共用，旧响应不许盖新）
6. **「异常」改走后端过滤**：`apps/api/src/modules/orders/routes.ts` `/client/orders` 新认 `statusGroup=attention`（用共享 `matchesShipmentListFilter`，不动 GROUP_ALIAS 五桶）；`apps/web/src/services/business-api.ts` `fetchClientOrders` 现在带参数、前端那道 `filter` 保留做兜底
7. `apps/web/src/modules/shared/request-gate.ts` 新增 `cancel()`；admin/staff 批量导入读文件的手写 `batchReadSequence` 换成 `batchReadGate`
8. `apps/web/src/services/core-api.ts` 401 分支对「没经 fetchWithSession 打标的响应」加开发期 console.warn；`scripts/test-session-api.ts` 新增源码扫描（调 parseApiResponse 的文件必须 `fetchWithSession as fetch`，带变异自证）；`ContainerTrackingSection.tsx` 改正误导注释

主修声称：三套 tsc 0 错，19 套 `npm run test:*` 全绿（test-attention-filter 8、test-client-status-group 19、test-session-api 27）。**next build 没跑**（3000 是老板在用的 dev 服务）。

### 任务一：8 处修复逐条销号（先做这个）

每条按「修对了没 / 修全了没 / 有没有改坏别的」三问：

- **#1 密码 trim**：`change-password` 路由（`auth/routes.ts`）存新密码**不 trim**，管理员路由 trim——现在前端登录 trim 之后，「自己改密码时两端带空格」的人是不是登不进了？这是不是本来就登不进（改前也 trim）？给出结论，别猜。
- **#3 员工端选中**：admin 与 staff 现在是否**逐行为等价**（计数、提示文案、导出 source、按钮 disabled 条件、全选 checked/indeterminate、toggleSelectAll）？漏一处就报。
- **#5 门闩**：`loadOrders` 是 `useCallback([], ...)`，gate 来自 `useRef(createRequestGate()).current`——闭包拿到的是同一个实例吗？验号是否覆盖成功/失败/finally 三分支（这里只有成功分支 setState，失败由调用方处理，判断是否够）？`refreshOrderList` 在旧响应被丢弃时仍弹「已刷新」toast，算不算误导？
- **#6 attention 走后端**：这是本轮**唯一动了 API 行为**的改动，往死里查：
  a. `GROUP_ALIAS` 不认的值仍「不过滤」——`attention` 没进 GROUP_ALIAS，靠 `wantAttention` 单独判，`wantedGroups` 此时 undefined，两个分支顺序对不对？
  b. 响应里每张单的 `statusGroup` 字段（阶段）是否**没被改写**（异常单仍显示在途/关闭）？
  c. `fetchAllPages` 按 `total` 翻页，而 `total` 是**过滤前**的 count（路由先 skip/take 再内存过滤）——异常单会不会因为落在后面的页而被漏？`test-attention-filter` 第 3 项（老后端整页返回）和第 4 项（新后端每页只回异常单）两种夹具是否真覆盖了「异常单分散在 2 页」的情况？
  d. 老前端缓存（没 Ctrl+F5）+ 新后端、新前端 + 老后端（部署顺序）两种错配下行为是否都正确？
  e. 员工端/管理员端的列表不走这个接口——它们的「异常」筛选是纯前端 `matchesShipmentListFilter`，三端口径是否仍一致？
- **#7 cancel()**：admin/staff 两页的取消按钮、关弹窗、卸载三处是否都调了 `batchReadGate.cancel()`；staff 那两个 early-return（超大小 / 超行数）路径下 `setBatchFileReading(false)` 是否仍正确（一个显式、一个靠 finally）。
- **#8 扫描测试**：`scripts/test-session-api.ts` 最后一项的正则——`parseApiResponse<T>(` 带泛型能认吗？`globalThis.fetch(` 会不会误报？注释里出现 `fetch (` 会不会误报？变异自证是不是真的能红？把它改坏（比如去掉 `!source.includes(...)`）跑一次看红不红。

### 任务二：对白天 12 轮再扫一遍（主修可能有漏）

主修那轮 8 路审查报了 10 条。你独立再扫，重点：

1. **三端一致性**（CLAUDE.md #20）：admin / staff / client 三个 page.tsx 是各自手写的。本轮改的每个功能（筛选单行、固定列、精度、导出弹窗、复制单号、批量导入读文件、清空条件、页码回夹、选中计数）逐一列「三端各自有没有、行为一样不一样」的表。
2. **精度只改展示**：grep 所有 `toFixed(` 和 `formatMetric(` 新增/改动处，确认没有任何一处落在提交 payload、计算、Excel 导出原值上。客户预报单提交的 `volumeM3` 仍是 6 位？
3. **globals.css 大改（+813/−393）+ 新 ledger.css**：找「tsx 里用了但两份 css 都没定义」的 class（主修抓到 3 个已补，看还有没有）；找「css 定义了但没人用」的（主修知道 `.section-divider`、`.client-secondary-section`、`.workbench-button--danger`、`.badge-line`、`.route-visual` 是死的，这几个不用报）。
4. **会话改动**（`auth-session.ts`、`core-api.ts`、`RoleShell.tsx`、`login/page.tsx`）：新的「认主人」逻辑有没有把 08-25 加的封号/改密即失效、08-31 的退出黑名单这些既有防线绕过去？跨标签换身份时 `window.location.reload()` 是否可能死循环？
5. **request-gate 三心法**：本轮**新增**的所有异步取数→setState 路径（不是白天已有的），有没有没领号/没验号/失败分支没验的。
6. **新测试是不是假绿**：`test-session-stability`、`test-session-api`、`test-attention-filter` 三套是真调模块（vm 里跑真源码）还是 grep 源码。把核心逻辑包进 `if (false)` 试一次。

### 任务三：不要报的（已拍板 / 待拍板 / 已知）

- 打开登录页清不清上一个人的会话：**待老板选边**（今天的改法反转了 8-31 第 57 条），别当 bug 报，也别替他选。
- `.a3-shell`（258 条）与 `.ledger-shell`（66 个 !important）两层主题叠加、`RoleShell` 的 `variant` 是死参数：**已知，留待有真数据截图时单独做**。
- `ai_status_labels` 里「中国仓已入库」命名、员工弱口令、仓库公开、陌生 SSH 钥匙、第三方容器、图片 URL 无鉴权：老板已知悉，不报。
- 9 张父单、92MB 重复图等数据问题：已作废，不报。
- 客户端老页面缓存要强刷才见六分组：已知。

### 规矩

- 可跑：三套 `tsc --noEmit`（`apps/api/tsconfig.json`、`apps/web/tsconfig.json`、`tsconfig.scripts.json`）、19 套 `npm run test:*`（都不连库）、无库夹具。
- **`next build` 只许在副本里跑**（`rsync -a --exclude=node_modules --exclude=.next` 到临时目录再 build），本仓库的 `apps/web/.next` 是老板正在用的 3000 dev 服务的，砸了他就掉线。
- 不许连数据库（Neon 今晚本机也连不上，Prisma P1001，别浪费时间）、不许起服务、不许碰 3000/3001、不许 git 写操作、不许改仓库文件（要验变异自证在副本里改）。
- 报告先说「我实测过」还是「读代码推的」；查不到就说没查，别用「应该是」。

### 输出

① 任务一 8 处销号表（✅ 修对 / ⚠️ 修了但不全 / ❌ 修错，各附 file:line 与依据）；
② 任务二发现清单（P1/P2/P3 + file:line + 触发序列 + 建议修法），没有就明说「没有」；
③ 三端一致性表；
④ 实跑记录（命令、退出码、关键输出原样）；
⑤ 最终一句话：**可以提交 / 先修再提交**（注意是提交不是部署——部署由老板另行决定）。

---

## 第二轮：复核你报的 5 条是否修干净（2026-09-06）

> 用法：整段复制给 Codex。范围仍是 `git diff HEAD` + 未跟踪文件，HEAD 仍是 `4691a4e`。

你上一轮报了 P2-1 / P2-2 / P2-3 / P3-1 / P3-2 加一条注释备注，主修声称全部处理：

1. **P2-1** `apps/web/src/app/admin/page.tsx` `loadOrders` 改为返回 `"applied" | "stale"`：catch 里先验号（作废请求的失败直接 return "stale" 不抛），成功写行后返回 "applied"；`refreshOrderList` 只在 applied 时弹「运单列表已刷新」，stale 弹「列表已由自动刷新更新」。用你上一轮的两个时序（R1 手动→R2 轮询→R1 晚成功；R2 先提交→R1 迟到失败）重跑你的真源码回调夹具，确认两条都闭合。另查 7 个调用 `loadOrders()` 的地方有没有因返回值变化出问题。
2. **P2-2** `scripts/test-session-api.ts` 最后一项改成 TypeScript AST 扫描（ts.createSourceFile + 遍历 CallExpression / ImportDeclaration）。把你上一轮那张五行表（简单泛型 / globalThis.fetch / 嵌套泛型 / 注释里的别名 / 注释里的 `fetch (` + 显式 sentToken）在副本里逐行重放，看现在是否全部判对；再想两个它可能还漏的形状（比如 `const f = fetch; f(...)`、`window['fetch']`、动态 import 的 fetch）——漏了就报，但注明是否在当前仓库里真的出现过。
3. **P2-3** `scripts/test-attention-filter.ts` 新增「REAL route + real client service」用例：真 `GET /client/orders` 处理函数 + 认 skip/take 的 Prisma 内存桩，620 单、异常单散在两页 3/5。主修实跑过变异：禁掉 `orders/routes.ts` 里 `wantAttention` 分支 → `FAILURES 1`（回 [500,120]）。你在副本里重做这个变异，再加一个：把 `total` 改成筛后数，看会不会红。
4. **P3-1 / P3-2 / 注释**：员工导出提示文案、`.section-*` 五条死样式、登录页 :47 那条旧注释——核一眼即可。

规矩同第一轮（不连库、不碰 3000/3001、build 只在副本、不改仓库文件）。输出：① 5 条销号表；② 新问题（有就 P1/P2/P3 + file:line + 触发序列，没有就写「没有」）；③ 实跑记录；④ 一句话：**可以提交 / 先修再提交**。

---

## 第三轮：只核你第二轮剩下的两条（2026-09-06）

> 用法：整段复制给 Codex。范围不变。

1. **P2 stale 提示**：`apps/web/src/app/admin/page.tsx` `refreshOrderList` 里 stale 分支的文案改成「本次刷新已被较新的请求替代」——不再宣称更新完成。用你那 6 场景真源码回调夹具重跑（尤其 R1 手动→R2 轮询→R1 作废→R2 失败 那条），确认 6/6。
2. **P3 扫描扩展**：`scripts/test-session-api.ts` 的 AST 扫描改成按**引用**算裸 fetch（不只按调用）：没别名时任何位置的标识符 `fetch`（import 说明符、`x.fetch` 属性名、对象字面量键除外）都算；`globalThis/window/self` 上的 `.fetch` 与 `['fetch']` 不管有没有别名都算。自证用例加了 5 条（`const f = fetch; f()`、`window['fetch']`、`globalThis["fetch"]` 存变量、别名后的 `const f = fetch` 合规、对象键 `{ fetch: 1 }` 合规）。把你副本里那两条漏网夹具重放，确认退出 1；再看整仓库有没有被误伤（`typeof fetch` 之类）。

输出：两条销号 + 一句话：可以提交 / 先修再提交。

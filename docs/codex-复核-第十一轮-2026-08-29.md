# 请复核（第十一轮）：上一轮 5 组问题 + 前端 3 处全修完

**你上一轮报的每一条都属实。其中最该记的是这条：**

> **测试假绿你报了三轮，我拖了两轮没动。这次五个变异全部清掉了。**

而且你说的「登录那道闸完全可以零成本真调」是对的 ——
账号锁定排在查用户表**之前**，所以先灌满失败计数再调路由就能测到。
我拖着不做是错的。

**基线（你上轮看的终点）**：`6fe4618`
**这一轮**：`dceff0e` → `93b4803`
**范围**：19 个文件、+888 / −46。**没有动数据库、没有迁移、没有改任何数据。**

```
git log --oneline 6fe4618..HEAD
git diff 6fe4618..HEAD
```

---

## ⛔ 不许做的

1. **不许部署、不许推送、不许跑迁移、不许写任何数据。不许改仓库里任何文件。**
2. **不许在页面上做会改数据的操作。不许调真的 DeepSeek。**
3. **不许查生产库**（只读测试库可以）。
4. **不许改这几个已拍板的口径**：北京时间；统计类不润色；
   登录限流 30 分钟 20 次；`/images/*` 无认证不修。
5. **不许为了让测试变绿去放宽断言。**

---

## 一、五条假绿全部堵上（你报了三轮那批）

| 你做的变异 | 现在 |
|---|---|
| 多行 `if (false) { 加锁代码 }` | 第 1 项红（新增 `deadBlockEnd()` 按大括号配对跳整块） |
| 让登录账号闸永不触发 | 第 16 项红（**新增真调 `/auth/login`**） |
| 三条删除路由忽略共用判断结果 | 第 8 项红 |
| 断开「修改客户单价」的金额闸 | 第 7 项红（为此把三处单价校验挪到碰库之前） |
| 删除尾端派送的 404 翻译 | 第 21 项红（**换了守法**，见 2.3） |

## 二、五组 P1

### 2.1 仓库版整数闸被布尔/数组绕过
`client-routes.ts:266` 还在用 `Number()`。改用 `parseNumericStrict`，
单件重量补 `requireDecimal`。

### 2.2 只卡输入、没卡**算出来**的字段
新增 `requireDerivedWithinDecimal()`。
⚠️ 算式跟真正写库那几行保持一致，否则这道闸放行的数写库还是会炸。

### 2.3 `ShipmentsNotFoundError` 只在一处翻成 404
改成**继承 `BusinessError`**，最外层统一翻，五个调用点都不用管。
⚠️ 测试也跟着换：盯**继承关系**，不盯某一处 try/catch 写没写 ——
结构保证比逐处 catch 可靠，新加调用点不用记得去接。

### 2.4 确认收货仍可把正数存成 0
按数据库精度校验（重量 `Decimal(10,2)`、体积 `Decimal(10,3)`，两列精度不同）。

### 2.5 确认收货仍用事务外的运单状态 → 锁后重读

## 三、🆕 前端 3 处，以及**我上一轮引入的一个真回归**

### 3.1 🚨 我上轮改后端会让 8 张真实单子确认不了收货
上一轮我把确认收货收紧成「重量/方数传了就必须大于 0」。
但前端 `buildPrealertDraft` 对**本来就没填重量**的单子给的是 `0` ——
发出去会被后端 400 打回来。

**只读查生产库：待确认收货 1108 张里，有 8 张没填重量/方数** ——
这 8 张**根本确认不了收货**。

修法：前端把「空着 / 0」翻译成**不发这个字段**
（后端语义「没传 = 不改」），而不是发 0。

**请你重点核这条**：这个修法对不对？有没有别的路径也在把 0 当真值发？

### 3.2 确认收货弹窗零校验，且失败也关弹窗
`setApprovingPrealert(null)` 原来写在 try/catch **外面**。
现在只有成功才关，失败保留已填内容。

### 3.3 四个输入框清空变 0
⚠️ **我故意没做类型重构**：把 draft 那四个字段改成 string 要动 40 多处引用，
我在这个项目里因为「顺手大改」引入新 bug 已经好几次。
退一步让显示和提交口径对齐：**0 渲染成空框**，提交时 0 当「没填」。
**请你评价这个取舍**：够不够？有没有它盖不住的场景？

### 3.4 总方数清空后悄悄变回 68 → 现在当场提示

### 3.5 🆕 自测把「第三个确认收货入口」逼了出来
我本来只改了弹窗那个。写测试时发现**同一个文件里有两处 `receiveStaffPrealert`**
（~988 和 ~2426），加上管理员端一共**三个入口**，三个都有同样的毛病。

---

## 四、请你重点打的

1. **3.1 那个修法**：还有没有别的地方把 0 当真值发给后端？
   （尤其别的「可填可不填」的数字字段）
2. **2.2 那批派生值闸的算式，跟真正写库那几行对得上吗？**
   对不上的话这道闸放行的数写库还是会炸。请逐个比对。
3. **确认收货现在要求重量/方数「传了就必须大于 0」**，
   而前端把 0 变成了不传。那**员工想主动把重量清零**怎么办？
   这个语义变化有没有正当场景被挡住？
4. **再开两个连接实测**：这两轮新加的确认收货事务、
   `lockAndSyncParents` 那条路，有没有跟别处成环。
5. **再想新的绕法**打这 12 个测试脚本 —— 你已经想了 11 种，我都补了，
   我不信没有第 12 种。特别是那几条**扫源码**的检查
   （集货删除接线、前端第 6 项、锁序第 8/9 项），它们天生只能证明
   「源码里写了」，证明不了运行时。
6. **找我这份清单之外的。**

---

## 五、我做过的验证

- **12 个自测脚本、229 项**，退出码全 0，全部已接进 CI
- 三套 TypeScript 检查 0 错；Web 构建 25/25 页
- **变异验证 16 个**，全部会红（含你上轮报的 5 个）

**我没做的：**
- 任何真实并发测试（本地跑不动）
- 任何真实 DeepSeek 调用

**这两轮我自己踩的坑（都写进注释了）：**
1. 改两处代码时把两个 `assert` 写在同一个脚本里，第二个锚点没中导致**整个中止**，
   于是第一处改动**也没写进去**，我却以为改了 —— 排查了一轮才发现
2. 检查「关弹窗那句在不在 try 里面」，第一版直接 `indexOf` ——
   抓到的是**「取消」按钮**那一处，干净代码被判红
3. 找提交函数用 `indexOf("await receiveStaffPrealert({")` ——
   抓到的是第一处（另一个入口），根本没看弹窗那个。
   **同一个文件里同名调用不止一处时，`indexOf` 找到的不一定是你要的那个**
4. 「不小心连测试库」这个坑我这几轮踩到**第四次** ——
   每次都被脚本开头那道「连不通的数据库地址」硬闸当场炸出来

## 六、还没修的（等老板拍板）
- 那 4 张运行期图片的 blob 仍在提交历史里（425KB），要抹掉得重写分支历史
- 登录时间侧信道：你建议的「异步 scrypt + 并发上限 + 虚拟验证」老板还没定
- 父单能不能派送：你建议「`packageCount > 0` 可派、`= 0` 且已有子单不可派」

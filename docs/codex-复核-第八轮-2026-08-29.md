# 请复核（第八轮）：上一轮 3 组 P1 + 3 条 P2 全修完

**你上一轮报的六条，我逐条复现，全部属实，一条误报都没有。**
而且其中**两条是我上一轮自己造出来的**：

1. **卸柜那把运单锁就是我上一轮加的** —— 加在了柜内记录**之后**，
   跟装柜（运单在前）方向相反，等于我亲手造了一个死锁。
2. **删订单按 id 混排父单子单**也是我上一轮写的，
   `[...allIds].sort()` 看着整齐，但**整齐 ≠ 跟别人一致**。

**这一轮我又自己假绿了三次**（详见第 6 节），所以请**默认我改错了**。

**基线（你上轮看的终点）**：`68c1a78`
**这一轮**：`1fa2674` → `07ad5c8` → `e82ea85`
**范围**：11 个文件、+800 / −66。**没有动数据库、没有迁移、没有改任何数据。**

```
git log --oneline 68c1a78..HEAD
git diff 68c1a78..HEAD
```

---

## ⛔ 不许做的

1. **不许部署、不许推送、不许跑迁移、不许写任何数据。** 结束时 `git status` 要跟开始一致
   （应当只剩原来那 4 张未跟踪图片）。
2. **不许在页面上做会改数据的操作。**
3. **不许调真的 DeepSeek**（要花钱）。
4. **不许改这几个已拍板的口径**：
   - 「今天/本月」按北京时间；「这个月发了多少货」= 这个月**下的单**（`createdAt`）
   - 统计类和查单类回答**不交给模型润色**，服务问答照旧润色
   - 登录限流 **30 分钟 20 次**
5. **`/images/*` 无认证这条老板已拍板不修，不要报。**
6. **不许为了让测试变绿去放宽断言。**

---

## 一、产品数量：三个入口全接上了（`1fa2674`）

你实测 `/client/prealerts` 传 `packageCount: 0` → 200、三处存成 1。我复现了。
三个后端入口（员工建单 / 客户建单 / 管理员改单）现在都调 `validateProductRows`，
兜底 `|| 1` 和 `Math.max(1, ...)` 全部去掉，校验一律放在**碰数据库之前**。

**请你重点挑：**
1. **还有没有第四、第五个入口。** 我是靠 grep `packageCount || 1` 找的，
   万一别处用的是别的写法（`?? 1`、`Math.max(p.packageCount, 1)`、
   在前端就补好了再发）我就漏了。请你从**能写 order_products / shipments 件数**
   的方向反查一遍，而不是顺着我的写法找。
2. **仓库版集货那条我判定为「死代码、不用改」** ——
   `whr-consolidation/client-routes.ts:310/351` 的 `Number(it.packageCount) || 1`，
   理由是同文件 ~256 行已经卡了 `pkg <= 0 → 400`。
   **这条路的方数要按「方数 × 单价」收费，请你替我核一遍这个判断。**
   另外它只卡了 `> 0`、**没卡整数**，`2.5 箱` 能进去 —— 这算不算问题？
3. **客户建单我新加了「没有产品行时 packageCount 必须是正整数」。**
   会不会有正当的客户单子本来就不填箱数、现在建不了了？
4. 新增的 32 位整数上限（2147483647）：有没有哪个字段其实是 `BigInt` 或 `Decimal`，
   被我按 `Int` 卡窄了？

## 二、两条死锁（`e82ea85`）

### 2.1 分柜 vs 卸柜
统一到【柜 → 运单 → 柜内记录】。理由：装柜插的是一行**还不存在**的柜内记录，
没法提前锁它，所以只能让卸柜那边让步。卸柜的父单锁也一起提前了。

### 2.2 删订单
两条删订单路径都改成「先锁完全部子单（按 id 排）→ 再锁全部父单（按 id 排）」。

**请你重点挑（这一节最要紧）：**
1. **请再开两个连接实测一遍**，确认这两条真的没了。
2. **有没有因为改动引入新的环。** 我把卸柜的父单锁**提前**了，
   现在它是【柜 → 子单 → 父单 → 柜内记录】；
   请把所有会同时碰 `containers` / `shipments` / `shipment_container_items`
   的路径列出来对一遍。
3. **删订单的「先子后父」判定靠 `parentTrackingNo` 是否为空。**
   如果一张单既是别人的子单、又是另一张的父单（多层分柜），这个二分法就不对了。
   生产上有没有这种数据？如果有，正确的锁序该怎么定？
4. **全局锁序现在是【订单 → 柜 → 派送单 → 运单(子→父) → 柜内记录】。**
   这个顺序本身站得住吗？有没有哪条路径天然做不到？

## 三、登录限流：我推翻了自己上一版（`07ad5c8`）

你报的洞（`Admin` 登录成功清掉 `admin` 的计数桶）我复现了。
**修法是把小写去掉，按账号原样计数。**

理由：攻击者拿错的大小写去猜**根本没有意义** —— `xt001` 在库里查不到这个人，
不管密码对不对都是 401，他得不到任何关于密码的信息，只能死磕正确的那个写法。
所以我上一版「怕换大小写躲开计数」这个担心本身不成立，
反倒是转小写把「清零」也变宽了，开了个真洞。

**请你重点挑：**
1. **上面这个推理对不对。** 有没有哪种情况，攻击者用**错的大小写**
   仍然能拿到有用的信息（比如报错文案不同、响应时间不同、
   或者某个中间件把账号规范化过）？
2. 我中间还走过一版错的：「两个桶（小写+原样），成功只清原样桶」——
   被自测第 13 项逮住（本人打错几次后，小写桶永远清不掉，把自己关在门外）。
   **现在这版有没有同类的「把正常人关在门外」的路径？**
3. 三个失败出口都记了，你上轮确认没有第四个。**改动之后请再确认一遍。**

## 四、扫描器：你报的三个变异 + 我自己挖出来的第四个

1. `.sort()` 后接 `.reverse()` → 现在要求**以 `.sort(...)` 结尾**
2. `if (false) await ... FOR UPDATE` → 写死的假条件里的锁不算数
3. 多行 raw SQL `UPDATE` → 现在往后看到反引号收尾
4. 🆕 **「写一张表」本身就是在拿它的锁** —— 这条是我自己挖出来的，也是最要紧的：
   分柜只 `create` 柜内记录、不发 `FOR UPDATE`，
   所以 2.1 那个**真死锁在我的扫描器里当时是绿的**。
   现在 create/update/delete 也计入锁序。

另外「同一张表锁两次」改成只记第一次（同事务重锁是免费的）。

**新增第 8 项**盯「父单子单混排」。这条静态分析真解决不了（要查库才知道哪个是父单），
所以做成**审阅登记表**：新出现的批量锁一律先红，逼人读一遍写下理由再登记。
现在登记了 3 处，每处写了「这批 id 里为什么不会同时出现父单和它的子单」。

**请你重点挑：**
1. **再想三个能绕过它的变异。** 上一轮你想的三个我全补了，
   但我怀疑还有 —— 比如把锁写进 helper 函数、写进 `Promise.all`、
   `switch` 分支、三元表达式、或者把 `.sort()` 挂在变量声明那一行。
2. **第 8 项那三条登记的理由对不对。** 我写的是「这批 id 里不会同时出现
   父单和它的子单」，请你去数据库里**真查一遍**有没有反例
   （比如同一个柜里既装了父单又装了它的子单）。
3. **`MODEL_TABLE` 只映射了 5 张表。** 漏了哪张会出事的？
4. **`isDeadLine` 只认写死的 false/0/!true。** 这个补丁是不是太窄，
   有没有更值得防的「看着会执行、其实走不到」的写法？

## 五、响应格式 + 整数上限
- 补了方括号写法 `rawRes["write"](JSON.stringify(...))`。
  **请你再想别的绕法**（`Reflect.apply`、解构出方法名、
  `const m = "write"; rawRes[m](...)`）。
- 产品数量补了 32 位整数上限，边界两头（2147483647 / 2147483648）都测了。

## 六、⚠️ 这轮我自己假绿了三次（请重点验这三处的修法）

### 6.1 扫源码找 `validateProductRows(` → 包进 `if (false)` 照样全绿
死代码里的调用照样被扫到。**跟你在锁序扫描器上报的是同一件事。**
改成**真调路由**（照 `test-ai-chat-limits.ts` 那套注册进假 app）。

### 6.2 只断言「拿到 400」→ 关掉 `/staff/orders` 的校验，14 项照样全绿
因为那个请求缺别的必填项，落到下一道闸也是 400。
「拿到 400」证明不了「是被这道闸拦下的」。现在连**报错内容**一起对。

### 6.3 行为测试第一版**真的连上了 Neon 测试库**
跑到 `prisma.order.create` 才被外键拦住。
`test-ai-chat-limits.ts` 开头记着同一个教训，同一个坑第二次踩。
改成「产品行合法但故意缺 `warehouseId`」，停在连库之前那道闸。

**另外**第 11 项第一版**被我自己写的注释骗了**（注释里提到 `|| 1` 被当成代码）——
`test-lock-order` 上个月栽的是同一个坑。现在扫源码的检查一律先剔注释。

**请你在整个仓库里再找一遍这三类问题：**
- 扫源码冒充行为测试的
- 只断言状态码、不看报错内容的
- 号称「不连库」但其实会连库的（**请务必确认这一轮的新测试真的没连库**）

---

## 七、我做过的验证（供你判断哪些不用重做）

- 10 个自测脚本、**118 项**，退出码全 0，全部已接进 CI
- 三套 TypeScript 检查退出码 0；Web 构建 25/25 页
- **变异验证做了 15 个**，全部会红：
  三个入口各自包进 `if (false)`（3 个）/ 箱数放行 0 /
  登录转回小写 / 成功不清零 / 键混进 IP / 上限改 100 /
  `.sort()` 后接 `.reverse()` / `if (false)` 包住锁 / 多行 raw UPDATE /
  卸柜改回旧顺序 / 删订单改回混排 / 响应格式方括号写法 /
  客户建单加回 `Math.max` 兜底

**我没做的：**
- 任何真实并发测试（要连库开两个事务，本地跑不动）——
  **这两条死锁是你实测出来的，我这边只有读代码复现 + 静态检查兜底**
- 任何真实 DeepSeek 调用
- 978 单大客户的 AI 耗时压测
- 多层分柜（父单本身又是别人的子单）这种数据到底存不存在

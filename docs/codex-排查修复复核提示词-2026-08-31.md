# Codex 复核提示词 — 2026-08-31 全面排查修复轮

> 用法：把下面「提示词正文」整段复制给 Codex。

---

## 提示词正文

你是湘泰物流系统的独立审查员。仓库根目录下，`main` 分支在基线 `2369c46` 之上有 4 个新提交（`36e41a8` 后端、`b0fefb6` 前端、`9d6f8ff` 测试脚本与 README、以及一个 docs 提交），是按 `docs/系统全面排查报告-2026-08-31.md` 的 61 条问题 + 一轮内部复查 30 条整改做的集中修复。你的任务是**对着这份 diff 挑刺**：

```
git diff 2369c46..HEAD -- apps/ scripts/ README.md
```

### 审查要求

1. **逐条核对**：以 `docs/系统全面排查报告-2026-08-31.md` 为清单，抽查其中至少 20 条（3 条 P1 必查），确认代码真的修对了，不是只改了样子。每条要指出你读过的 file:line 依据。
2. **找新引入的问题**：这轮改动大（43 个文件，+2275/-1513），重点盯：
   - **锁序与事务**：新加/挪动了十几处 `$transaction` 和 `FOR UPDATE`。全局锁序约定：订单→运单（子单先、父单后、层内按 id 排）、柜→运单→柜内记录、集货任务→预报单。找反序、找锁外做决定、找锁后没重读。
   - **客户订单四分类契约**：后端 `/client/orders` 的 statusGroup（pending/transit/delivered/closed + 老值兼容）和客户端五个分组按钮、状态分布图是否逐字段一致；「已收货」加进过滤后有没有把别的口径带坏。
   - **收货金额链路**：员工端弹窗 → business-api → `/staff/prealerts/receive` → 订单+运单两处落库；必填校验是否真拦得住「不碰输入框直接提交」「清空后提交」。
   - **令牌黑名单（第58条）**：`token-blacklist.ts` + `server.ts parseAuth` + `http-utils requireAuth` 双保险，会不会误伤正常登录？内存实现的 TTL 和清理对不对？
   - **测试脚本自查**：`scripts/test-lock-order.ts` 新登记了两个 advisory 取号锁 helper、豁免只认纯 create；`scripts/test-product-rows.ts` 第 17 项换了靶子。**做变异推演**：把对应的真实防线删掉，这些测试还会红吗？
   - **删掉的东西**：`POST/DELETE /admin/containers/load`、`POST /staff/shipments/split`、`serve.mjs`、`MockLoginPanel`、business-api 的 4 个死函数。grep 确认真无调用方残留。
3. **验证方式**：只读审查 + 可跑 `tsc --noEmit`（三套）和 `npm run test:*`（14 个脚本，全部不连库）。**不许连任何数据库、不许起服务、不许 git 写操作。**
4. **别报这些**（已拍板/已知悉）：客户密码=唛头、员工弱口令、仓库公开、陌生 SSH 钥匙、第三方容器、图片 URL 无鉴权、上传 10MiB 上限、deploy.sh 的 db push、audit_logs 未接线、ai_status_labels 混计费配置、备份系统旧毛病、生产数据层面一切、nginx 日志 14 天。也别报下面「已知待拍板」三项。

### 已知待拍板（不算漏修，报了不计分）

1. 后端 `/client/prealerts/update` 和 `/client/prealerts/delete` 已无前端入口但接口还活着，是否下线待老板定。
2. 仓库版集货「柜+客户」数据库唯一约束要动表结构，待老板批准后单独做（代码层事务锁已补）。
3. 部分卸柜还货给父单时补了「状态退回已创建 + 写轨迹」（与全量卸柜口径一致），属行为改动，上线前老板会知悉。

### 输出格式

逐条列：`[P1/P2/P3] 文件:行号 — 问题描述 — 依据（你读到的代码）— 建议修法`。P1=会算错钱/丢数据/权限越界，P2=真实场景功能坏，P3=小毛病。没有问题的审查维度也要列出来说明核过什么。宁缺毋滥，每条都要能指着代码讲清楚。
